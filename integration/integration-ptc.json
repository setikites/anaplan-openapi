{
  "catalog": [
    {
      "op_id": "getModels",
      "method": "GET",
      "path": "/models",
      "summary": "List user models (large responses \u2014 see description)",
      "parameters": [
        {
          "name": "modelDetails",
          "in": "query",
          "required": false,
          "type": "boolean"
        },
        {
          "name": "s",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelid",
      "method": "GET",
      "path": "/models/{modelId}",
      "summary": "Get single model metadata",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelDetails",
          "in": "query",
          "required": false,
          "type": "boolean"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidActions",
      "method": "GET",
      "path": "/models/{modelId}/actions",
      "summary": "List the actions in the model as a JSON array",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidCurrentperiod",
      "method": "GET",
      "path": "/models/{modelId}/currentPeriod",
      "summary": "Get current period (model-scoped)",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "putModelsByModelidCurrentperiod",
      "method": "PUT",
      "path": "/models/{modelId}/currentPeriod",
      "summary": "Set current period",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "date",
          "in": "query",
          "required": false,
          "type": "string"
        }
      ],
      "body_fields": [
        "date"
      ]
    },
    {
      "op_id": "getModelsByModelidDimensionsByDimensionidItems",
      "method": "GET",
      "path": "/models/{modelId}/dimensions/{dimensionId}/items",
      "summary": "Model-direct form: Retrieve all items in a dimension",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "dimensionId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "postModelsByModelidDimensionsByDimensionidItems",
      "method": "POST",
      "path": "/models/{modelId}/dimensions/{dimensionId}/items",
      "summary": "Model-direct form: Lookup dimension items by name or code",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "dimensionId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": [
        "codes",
        "names"
      ]
    },
    {
      "op_id": "getModelsByModelidExports",
      "method": "GET",
      "path": "/models/{modelId}/exports",
      "summary": "List the available export definitions as a JSON array",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidExportsByExportid",
      "method": "GET",
      "path": "/models/{modelId}/exports/{exportId}",
      "summary": "Get the metadata for an export definition",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "exportId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidFiles",
      "method": "GET",
      "path": "/models/{modelId}/files",
      "summary": "Get a list of files",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidFilesByFileidChunks",
      "method": "GET",
      "path": "/models/{modelId}/files/{fileId}/chunks",
      "summary": "Get Chunks Details (model-direct)",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "fileId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidImports",
      "method": "GET",
      "path": "/models/{modelId}/imports",
      "summary": "Get a list of import actions",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidImportsByImportid",
      "method": "GET",
      "path": "/models/{modelId}/imports/{importId}",
      "summary": "Get the metadata for an import definition",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "importId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidLineitems",
      "method": "GET",
      "path": "/models/{modelId}/lineItems",
      "summary": "Retrieve all line items in a model",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidLineitemsByLineitemidDimensions",
      "method": "GET",
      "path": "/models/{modelId}/lineItems/{lineItemId}/dimensions",
      "summary": "Retrieve dimensions IDs for a line item",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "lineItemId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidLineitemsByLineitemidDimensionsByDimensionidItems",
      "method": "GET",
      "path": "/models/{modelId}/lineItems/{lineItemId}/dimensions/{dimensionId}/items",
      "summary": "Retrieve dimension items for a line item",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "lineItemId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "dimensionId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidLists",
      "method": "GET",
      "path": "/models/{modelId}/lists",
      "summary": "Model-direct form: Retrieve Lists",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidListsByListid",
      "method": "GET",
      "path": "/models/{modelId}/lists/{listId}",
      "summary": "Model-direct form: Retrieve list metadata",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "listId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidModelcalendar",
      "method": "GET",
      "path": "/models/{modelId}/modelCalendar",
      "summary": "Get model calendar (model-direct)",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "putModelsByModelidModelcalendarFiscalyear",
      "method": "PUT",
      "path": "/models/{modelId}/modelCalendar/fiscalYear",
      "summary": "Update current fiscal year",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": [
        "year"
      ]
    },
    {
      "op_id": "getModelsByModelidModules",
      "method": "GET",
      "path": "/models/{modelId}/modules",
      "summary": "Retrieve IDs and names for all modules in a model",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "postModelsByModelidModulesByModuleidData",
      "method": "POST",
      "path": "/models/{modelId}/modules/{moduleId}/data",
      "summary": "Write cell data by coordinate to module",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "moduleId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": [
        "lineItemId",
        "lineItemName",
        "dimensions",
        "value"
      ]
    },
    {
      "op_id": "getModelsByModelidModulesByModuleidLineitems",
      "method": "GET",
      "path": "/models/{modelId}/modules/{moduleId}/lineItems",
      "summary": "Retrieve all line item metadata in a module",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "moduleId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "includeAll",
          "in": "query",
          "required": false,
          "type": "boolean"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidModulesByModuleidViews",
      "method": "GET",
      "path": "/models/{modelId}/modules/{moduleId}/views",
      "summary": "Retrieve IDs and names for views in a module",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "moduleId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "includesubsidiaryviews",
          "in": "query",
          "required": false,
          "type": "boolean"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidProcesses",
      "method": "GET",
      "path": "/models/{modelId}/processes",
      "summary": "List the process definitions available in a model as a JSON array",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidProcessesByProcessid",
      "method": "GET",
      "path": "/models/{modelId}/processes/{processId}",
      "summary": "Retrieve metadata for a process",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "processId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "showImportDataSource",
          "in": "query",
          "required": false,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidVersions",
      "method": "GET",
      "path": "/models/{modelId}/versions",
      "summary": "Retrieve version metadata",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "putModelsByModelidVersionsByVersionidSwitchover",
      "method": "PUT",
      "path": "/models/{modelId}/versions/{versionId}/switchover",
      "summary": "Set version switchover date",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "versionId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": [
        "date"
      ]
    },
    {
      "op_id": "getModelsByModelidViews",
      "method": "GET",
      "path": "/models/{modelId}/views",
      "summary": "Model-direct form: Retrieve IDs and names for all views in a model",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "includesubsidiaryviews",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidViewsByViewid",
      "method": "GET",
      "path": "/models/{modelId}/views/{viewId}",
      "summary": "Retrieve metadata for dimensions on a view",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "viewId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidViewsByViewidData",
      "method": "GET",
      "path": "/models/{modelId}/views/{viewId}/data",
      "summary": "Retrieve cell data for a view (large responses \u2014 see description)",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "viewId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "Accept",
          "in": "header",
          "required": false,
          "type": "string"
        },
        {
          "name": "format",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "pages",
          "in": "query",
          "required": false,
          "type": "array"
        }
      ]
    },
    {
      "op_id": "getModelsByModelidViewsByViewidDimensionsByDimensionidItems",
      "method": "GET",
      "path": "/models/{modelId}/views/{viewId}/dimensions/{dimensionId}/items",
      "summary": "Retrieve selected items in a dimension (large responses \u2014 see description)",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "viewId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "dimensionId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getUsers",
      "method": "GET",
      "path": "/users",
      "summary": "List users (large responses \u2014 see description)",
      "parameters": [
        {
          "name": "s",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getUsersMe",
      "method": "GET",
      "path": "/users/me",
      "summary": "Retrieve your user",
      "parameters": []
    },
    {
      "op_id": "getUsersByUserid",
      "method": "GET",
      "path": "/users/{userId}",
      "summary": "Retrieve user information",
      "parameters": [
        {
          "name": "userId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspaces",
      "method": "GET",
      "path": "/workspaces",
      "summary": "List user workspaces (large responses \u2014 see description)",
      "parameters": [
        {
          "name": "tenantDetails",
          "in": "query",
          "required": false,
          "type": "boolean"
        },
        {
          "name": "s",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceid",
      "method": "GET",
      "path": "/workspaces/{workspaceId}",
      "summary": "Get single workspace metadata",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "tenantDetails",
          "in": "query",
          "required": false,
          "type": "boolean"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidAdmins",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/admins",
      "summary": "List workspace administrators",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "postWorkspacesByWorkspaceidBulkdeletemodels",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/bulkDeleteModels",
      "summary": "Bulk delete models",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": [
        "modelIdsToDelete"
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModels",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models",
      "summary": "List workspace models",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidActionsByActionidTasks",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/actions/{actionId}/tasks",
      "summary": "Get a list of action tasks",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "actionId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "postWorkspacesByWorkspaceidModelsByModelidActionsByActionidTasks",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/models/{modelId}/actions/{actionId}/tasks",
      "summary": "Start Other Action",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "actionId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidActionsByActionidTasksByTaskid",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/actions/{actionId}/tasks/{taskId}",
      "summary": "Get the status of an action task",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "actionId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "taskId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidDashboards",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/dashboards",
      "summary": "List dashboards in a model",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidExportsByExportidTasks",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/exports/{exportId}/tasks",
      "summary": "Get a list of export tasks",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "exportId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "postWorkspacesByWorkspaceidModelsByModelidExportsByExportidTasks",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/models/{modelId}/exports/{exportId}/tasks",
      "summary": "Start the export",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "exportId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": [
        "localeName"
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidExportsByExportidTasksByTaskid",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/exports/{exportId}/tasks/{taskId}",
      "summary": "Monitor the export tasks",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "exportId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "taskId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidFilesByFileid",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/files/{fileId}",
      "summary": "Download file content in a single call (large responses \u2014 see description)",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "fileId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "postWorkspacesByWorkspaceidModelsByModelidFilesByFileid",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/models/{modelId}/files/{fileId}",
      "summary": "Set the chunk count",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "fileId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": [
        "chunkCount"
      ]
    },
    {
      "op_id": "putWorkspacesByWorkspaceidModelsByModelidFilesByFileid",
      "method": "PUT",
      "path": "/workspaces/{workspaceId}/models/{modelId}/files/{fileId}",
      "summary": "Upload file as a single chunk",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "fileId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": []
    },
    {
      "op_id": "deleteWorkspacesByWorkspaceidModelsByModelidFilesByFileid",
      "method": "DELETE",
      "path": "/workspaces/{workspaceId}/models/{modelId}/files/{fileId}",
      "summary": "Delete files",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "fileId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidFilesByFileidChunksByChunkid",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/files/{fileId}/chunks/{chunkId}",
      "summary": "Get the data in a chunk",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "fileId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "chunkId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "putWorkspacesByWorkspaceidModelsByModelidFilesByFileidChunksByChunkid",
      "method": "PUT",
      "path": "/workspaces/{workspaceId}/models/{modelId}/files/{fileId}/chunks/{chunkId}",
      "summary": "Upload a file in chunks",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "fileId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "chunkId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": []
    },
    {
      "op_id": "postWorkspacesByWorkspaceidModelsByModelidFilesByFileidComplete",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/models/{modelId}/files/{fileId}/complete",
      "summary": "Complete the upload",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "fileId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasks",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks",
      "summary": "Get a list of the import tasks",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "importId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "postWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasks",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks",
      "summary": "Start the import",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "importId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasksByTaskid",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks/{taskId}",
      "summary": "Monitor the import task",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "importId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "taskId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasksByTaskidDump",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks/{taskId}/dump",
      "summary": "Check dump file for failures",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "importId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "taskId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasksByTaskidDumpChunks",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks/{taskId}/dump/chunks",
      "summary": "Query the number of chunks in a dump file",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "importId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "taskId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasksByTaskidDumpChunksByChunkid",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks/{taskId}/dump/chunks/{chunkId}",
      "summary": "Download dump file chunks",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "importId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "taskId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "chunkId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidListsByListidItems",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/lists/{listId}/items",
      "summary": "Retrieve list data (large responses \u2014 see description)",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "listId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "includeAll",
          "in": "query",
          "required": false,
          "type": "boolean"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "postWorkspacesByWorkspaceidModelsByModelidListsByListidItems",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/models/{modelId}/lists/{listId}/items",
      "summary": "Add or delete list items",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "listId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "action",
          "in": "query",
          "required": true,
          "type": "string"
        }
      ],
      "body_fields": [
        "items"
      ]
    },
    {
      "op_id": "putWorkspacesByWorkspaceidModelsByModelidListsByListidItems",
      "method": "PUT",
      "path": "/workspaces/{workspaceId}/models/{modelId}/lists/{listId}/items",
      "summary": "Update list items",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "listId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "postWorkspacesByWorkspaceidModelsByModelidListsByListidReadrequests",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/models/{modelId}/lists/{listId}/readRequests",
      "summary": "Start a large-list read request",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "listId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidProcessesByProcessidTasks",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/processes/{processId}/tasks",
      "summary": "Get the list of tasks for the process",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "processId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    },
    {
      "op_id": "postWorkspacesByWorkspaceidModelsByModelidProcessesByProcessidTasks",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/models/{modelId}/processes/{processId}/tasks",
      "summary": "Start the process",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "processId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidProcessesByProcessidTasksByTaskid",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/processes/{processId}/tasks/{taskId}",
      "summary": "Monitor the process tasks",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "processId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "taskId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "postWorkspacesByWorkspaceidModelsByModelidViewsByViewidReadrequests",
      "method": "POST",
      "path": "/workspaces/{workspaceId}/models/{modelId}/views/{viewId}/readRequests",
      "summary": "Initiate long read request",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "viewId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidViewsByViewidReadrequestsByRequestid",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/views/{viewId}/readRequests/{requestId}",
      "summary": "Retrieve status of large read request",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "viewId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "requestId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "deleteWorkspacesByWorkspaceidModelsByModelidViewsByViewidReadrequestsByRequestid",
      "method": "DELETE",
      "path": "/workspaces/{workspaceId}/models/{modelId}/views/{viewId}/readRequests/{requestId}",
      "summary": "Delete Read Request",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "viewId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "requestId",
          "in": "path",
          "required": true,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidModelsByModelidViewsByViewidReadrequestsByRequestidPagesByPageno",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/models/{modelId}/views/{viewId}/readRequests/{requestId}/pages/{pageNo}",
      "summary": "Download Pages",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "modelId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "viewId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "requestId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "pageNo",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "Accept-Encoding",
          "in": "header",
          "required": false,
          "type": "string"
        }
      ]
    },
    {
      "op_id": "getWorkspacesByWorkspaceidVisitors",
      "method": "GET",
      "path": "/workspaces/{workspaceId}/visitors",
      "summary": "List workspace visitors",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "required": true,
          "type": "string"
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer"
        }
      ]
    }
  ],
  "detail": {
    "getModels": {
      "description": "Minimum role: Standard User.\n\nRetrieves information about all models in the user's default tenant.\n\nModels with `activeState` of `ARCHIVED` are typically inactive and not actionable. Filter them out of results (keep only `activeState` != `ARCHIVED`) unless you specifically need archived models.\n\n**HIGH VOLUME** \u2014 this returns every model in the user's default tenant in one merged response. With no `limit` a client that auto-follows `meta.paging` combines every page and can overflow its token budget. Reduce volume by passing an explicit `limit` and paging with `offset`, by scoping server-side (the `s=` search filter, or the workspace-scoped `GET /workspaces/{workspaceId}/models` list) instead of full enumeration, and/or by using `search` to locate the specific op/params rather than pulling the whole collection.",
      "parameters": [
        {
          "name": "modelDetails",
          "in": "query",
          "description": "When true, includes available model memory usage in bytes in the response.",
          "schema": {
            "type": "boolean"
          },
          "example": true
        },
        {
          "name": "s",
          "in": "query",
          "description": "Optional case-insensitive substring filter (matches anywhere in the field). Supports wildcards `%` (0-n characters) and `_` (exactly 1 character). Omit to return all items. **Undocumented, and behavior is endpoint-specific.** `GET /workspaces` and `GET /users` are verified with the Workspace Administrator role. `GET /models` requires the Tenant Admin role. Without that role it returns the full model list unfiltered.",
          "required": false,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort results. `+field` ascending, `-field` descending. The prefix is required and the field name is validated (bad or unknown field returns 400). Comma-separate to sort by multiple columns. Sortable fields: name, activeState.",
          "schema": {
            "type": "string"
          },
          "example": "+name"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "models": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string",
                    "pattern": "^[0-9a-fA-F]{32}$"
                  },
                  "name": {
                    "type": "string"
                  },
                  "activeState": {
                    "type": "string",
                    "enum": [
                      "MAINTENANCE",
                      "UNLOCKED",
                      "PRODUCTION",
                      "ARCHIVED",
                      "PRODUCTION_MAINTENANCE",
                      "LOCKED"
                    ]
                  },
                  "currentWorkspaceId": {
                    "type": "string"
                  },
                  "currentWorkspaceName": {
                    "type": "string"
                  },
                  "modelUrl": {
                    "type": "string"
                  },
                  "categoryValues": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "attribute": {
                          "type": "string"
                        },
                        "categoryId": {
                          "type": "string"
                        },
                        "categoryName": {
                          "type": "string"
                        },
                        "customerId": {
                          "type": "string"
                        }
                      }
                    }
                  },
                  "isoCreationDate": {
                    "type": "string"
                  },
                  "lastModified": {
                    "type": "string"
                  },
                  "lastModifiedByUserGuid": {
                    "type": "string"
                  },
                  "lastSavedSerialNumber": {
                    "type": "string"
                  },
                  "memoryUsage": {
                    "type": "number"
                  },
                  "modelTransactionRunning": {
                    "type": "boolean"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelid": {
      "description": "Minimum role: Standard User.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelDetails",
          "in": "query",
          "schema": {
            "type": "boolean"
          },
          "example": true
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "model": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "pattern": "^[0-9a-fA-F]{32}$"
                },
                "name": {
                  "type": "string"
                },
                "activeState": {
                  "type": "string",
                  "enum": [
                    "MAINTENANCE",
                    "UNLOCKED",
                    "PRODUCTION",
                    "ARCHIVED",
                    "PRODUCTION_MAINTENANCE",
                    "LOCKED"
                  ]
                },
                "currentWorkspaceId": {
                  "type": "string"
                },
                "currentWorkspaceName": {
                  "type": "string"
                },
                "modelUrl": {
                  "type": "string"
                },
                "categoryValues": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "attribute": {
                        "type": "string"
                      },
                      "categoryId": {
                        "type": "string"
                      },
                      "categoryName": {
                        "type": "string"
                      },
                      "customerId": {
                        "type": "string"
                      }
                    }
                  }
                },
                "isoCreationDate": {
                  "type": "string"
                },
                "lastModified": {
                  "type": "string"
                },
                "lastModifiedByUserGuid": {
                  "type": "string"
                },
                "lastSavedSerialNumber": {
                  "type": "string"
                },
                "memoryUsage": {
                  "type": "number"
                },
                "modelTransactionRunning": {
                  "type": "boolean"
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidActions": {
      "description": "Minimum role: Standard User.\n\nModel-direct form of `GET /workspaces/{workspaceId}/models/{modelId}/actions`. Returns the same response shape without requiring a workspace ID. Lists delete actions available for the specified model.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort results. `+field` or unprefixed ascending, `-field` descending. Comma-separate for multiple columns. `name` and `id` reorder results. Field name not validated \u2014 unknown fields return 200 and fall back to default order. Example: `+name`, `-id`.",
          "schema": {
            "type": "string"
          },
          "example": "+name"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "actions": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  },
                  "code": {
                    "type": "string"
                  },
                  "actionType": {
                    "type": "string",
                    "description": "Category of action. Observed values include DELETE_BY_SELECTION. Distinguishes deletion actions from imports, exports, and processes."
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidCurrentperiod": {
      "description": "Minimum role: Workspace Administrator.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "currentPeriod": {
              "properties": {
                "calendarType": {
                  "type": "string"
                },
                "lastDay": {
                  "type": "string"
                },
                "periodText": {
                  "type": "string",
                  "description": "Human-readable label for the current period. Empty string when no current period is set.",
                  "example": "Jan 24"
                }
              }
            }
          }
        }
      }
    },
    "putModelsByModelidCurrentperiod": {
      "description": "Minimum role: Workspace Administrator.\n\nChanges or resets the current period in Anaplan. When this call contains a specified date (for example, 2020-05-20), the API determines what period range contains the specified data and sets that range as the current period.  When the date value is set to blank in this call, the API resets the current period to blank.\n\n**Warning**: A change to your time settings is a potentially destructive action and may lead to data loss.  The new range of time periods may not include previous time periods. Any data in the removed time periods is deleted from the model.\n\n**Notes**:\n\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response\n\n### Request body\n\n| **Body** | **Details** |\n| --- | --- |\n| `{date}` | <br><br>*   Required<br>*   Type: String<br>*   Description: The date to use to set, or reset, the current period. The date format is YYYY-MM-DD.  <br>    If the `date` parameter is left blank (for example, `/currentperiod?date=`) the current period resets.  <br>    If the date is not provided in the YYYY-MM-DD format, the call returns a `400 Bad Request` response with this message: `Invalid ISO date format '{date}'. Date should match format YYYY-MM-DD`  <br>    If this value falls outside of a valid range, the call returns a `400 Bad Request` response with this message: `Specified date '{date}' is out of timescale range {start of timescale} - {end of timescale}`.<br>*   Example: 2010-05-20<br><br> |",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "date",
          "in": "query",
          "description": "The date used to set the current period (YYYY-MM-DD). Leave blank (?date=) to reset the current period. Accepted as either a query parameter or a request body field, but not both simultaneously. Returns 400 if the format is invalid or the date falls outside the model timescale.",
          "required": false,
          "schema": {
            "type": "string",
            "format": "date",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
          },
          "example": "2020-05-20"
        }
      ],
      "request_body": {
        "media_type": "application/json",
        "required": false,
        "schema": {
          "properties": {
            "date": {
              "format": "date",
              "type": "string"
            }
          },
          "type": "object"
        }
      },
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "currentPeriod": {
              "properties": {
                "calendarType": {
                  "type": "string"
                },
                "lastDay": {
                  "type": "string"
                },
                "periodText": {
                  "type": "string",
                  "description": "Human-readable label for the current period. Empty string when no current period is set.",
                  "example": "Jan 24"
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidDimensionsByDimensionidItems": {
      "description": "Minimum role: Standard User.\n\nAlternate URL form \u2014 response is equivalent to `/workspaces/{workspaceId}/models/{modelId}/dimensions/{dimensionId}/items`.\n\nReturns the IDs, codes, and names of the items in a dimension. The dimension must be a list, a list subset, a line item subset, or the Users dimension.\n\nCalls at model-level do not support the Time dimension. The calls return a `400` response. To avoid this, use the view dimension instead.\n\n**Notes**:\n\n*   This call is on the model-level. It returns all dimension items as it does not have view-level hiding, filtering, or [Selective Access](https://help.anaplan.com/anapedia/Content/Modeling/Users/Selective_Access.html). For the view-level endpoint, see Retrieve selected items in a dimension.\n*   If codes have not been set for an item, then the response excludes the `code` field.\n*   This call only supports the retrieval of dimensions and items from a dimension that contains a maximum of 1,000,000 items.  \n    The call returns a `400` response for any more than that number of items.\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response.\n*   When the dimension has no items, the response omits the `items` key entirely rather than returning an empty array.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "dimensionId",
          "in": "path",
          "description": "Dimension identifier. From `GET /models/{modelId}/lineItems/{lineItemId}/dimensions`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "items": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "postModelsByModelidDimensionsByDimensionidItems": {
      "description": "Minimum role: Standard User.\n\nModel-direct alternate URL form \u2014 identical behaviour to `/workspaces/{workspaceId}/models/{modelId}/dimensions/{dimensionId}/items` POST.\n\nRetrieve the items from a dimension that match one of a list of names or a list or codes.\n\n** Notes: **\n\n*   This call does not return results for names or codes for which an item does not exist.\n*   If the given workspace, model, or dimension do not exist, this call returns a `404` code.\n*   If the dimension does not support codes and codes are provided, this call returns a `400` code.\n*   In the future, use of this call may be rate limited.\n\n### Request body\n\n| **JSON Element** | **Description** |\n| --- | --- |\n| `names` | The case-insensitive strings that match the name of a returned item. |\n| `codes` | The case-insensitive strings that match the code of a returned item. |\n\nNote that the `names`, `codes`, or both fields in the request body must contain a non-empty value. If the request body does not include at least one of these fields, the response returns an error.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "dimensionId",
          "in": "path",
          "description": "Dimension identifier. From `GET /models/{modelId}/lineItems/{lineItemId}/dimensions`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": {
        "media_type": "application/json",
        "required": false,
        "schema": {
          "type": "object",
          "properties": {
            "codes": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "names": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      },
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "items": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidExports": {
      "description": "Minimum role: Standard User.\n\nModel-direct form of `GET /workspaces/{workspaceId}/models/{modelId}/exports`. Returns the same response shape without requiring a workspace ID. Lists export definitions available for the specified model.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort results. `+field` or unprefixed ascending, `-field` descending. Comma-separate for multiple columns. `name` and `id` reorder results. Field name not validated \u2014 unknown fields return 200 and fall back to default order. Example: `+name`, `-id`.",
          "schema": {
            "type": "string"
          },
          "example": "+name"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "exports": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "exportType": {
                    "type": "string",
                    "description": "Shape and scope of the exported data.",
                    "enum": [
                      "AUDIT_LOG",
                      "GRID_CURRENT_PAGE",
                      "TABULAR_ALL_LINE_ITEMS",
                      "TABULAR_CURRENT_LINE_ITEM",
                      "TABULAR_MULTI_COLUMN"
                    ]
                  },
                  "id": {
                    "type": "string",
                    "description": "12-digit numeric identifier. The prefix 116 marks this as an export definition. The export output file shares this same ID."
                  },
                  "name": {
                    "type": "string"
                  },
                  "encoding": {
                    "type": "string"
                  },
                  "exportFormat": {
                    "type": "string"
                  },
                  "layout": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidExportsByExportid": {
      "description": "Minimum role: Standard User.\n\nModel-direct form of `GET /workspaces/{workspaceId}/models/{modelId}/exports/{exportId}`. Returns the same `exportMetadata` response shape without requiring a workspace ID.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "exportId",
          "in": "path",
          "description": "Export definition identifier (12-digit numeric, prefix 116). From `GET /models/{modelId}/exports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "exportMetadata": {
              "properties": {
                "columnCount": {
                  "type": "integer"
                },
                "dataTypes": {
                  "items": {
                    "enum": [
                      "NONE",
                      "NUMBER",
                      "ENTITY",
                      "DATE",
                      "TEXT",
                      "BOOLEAN",
                      "FORMAT",
                      "SUMMARY",
                      "MULTISELECT_HIERARCHY",
                      "MULTISELECT_USER",
                      "MULTISELECT_ENTITY",
                      "MULTISELECT_MODULE",
                      "ACTION_DEFINITION",
                      "MIXED",
                      "TIME_ENTITY",
                      "REMOTE_MODEL_DEFINITION",
                      "MULTISELECT_TAG"
                    ]
                  },
                  "type": "array"
                },
                "delimiter": {
                  "type": "string"
                },
                "encoding": {
                  "type": "string"
                },
                "exportFormat": {
                  "type": "string"
                },
                "headerNames": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "listNames": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "rowCount": {
                  "type": "integer"
                },
                "separator": {
                  "type": "string"
                },
                "layout": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidFiles": {
      "description": "Minimum role: Standard User.\n\nTo upload a file, you need its ID in Anaplan:\n\n*   `id` is the ID that you need to upload the file.\n*   `name` is the filename of the file.\n*   `chunkCount` is the number of chunks in the file.\n*   `delimiter` is double-quotes for columns and fields. For example, \"Versions\",\"Opportunities\",\" \"Version 1\",\"Opportunity 0001\".\n*   `encoding` determines the way in which bytes in a file map to Unicode character code points. See [Common Character Encodings](https://en.wikipedia.org/wiki/Character_encoding#Common_character_encodings) on Wikipedia.\n*   `firstDataRow` is the first row after the row containing column names.\n*   `format` is text, as opposed to a binary stream.\n*   `headerRow` is the row that contains column names.\n*   `separator` is the token that separates fields. This is typically a comma, for example, \"Versions\",\"Opportunities\",\" \"Version 1\",\"Opportunity 0001\".\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Accept:application/json`",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort results. `+field` or unprefixed ascending, `-field` descending. Comma-separate for multiple columns. `name` and `id` reorder results. Field name not validated \u2014 unknown fields return 200 and fall back to default order. Example: `+name`, `-id`.",
          "schema": {
            "type": "string"
          },
          "example": "+name"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "files": {
              "type": "array",
              "items": {
                "description": "A file associated with an import data source, import failure dump, or export",
                "properties": {
                  "chunkCount": {
                    "type": "integer"
                  },
                  "code": {
                    "type": "string"
                  },
                  "country": {
                    "type": "string"
                  },
                  "delimiter": {
                    "type": "string"
                  },
                  "encoding": {
                    "type": "string",
                    "description": "Character encoding of the file. Observed values: ISO-8859-1, UTF-16LE, UTF-8."
                  },
                  "firstDataRow": {
                    "type": "integer",
                    "description": "Row number of the first data row. 0 means no data rows are configured. Other values are 1-based (row 1 = first row of the file)."
                  },
                  "format": {
                    "type": "string"
                  },
                  "headerRow": {
                    "type": "integer",
                    "description": "1-based row number of the column-header row in a delimited file."
                  },
                  "id": {
                    "type": "string",
                    "description": "12-digit numeric identifier. Import source files use the prefix 113. Export output files use 116, matching their parent export's ID."
                  },
                  "language": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  },
                  "origin": {
                    "type": "string",
                    "description": "Source type of the file: 'NATIVE_ANAPLAN' for Anaplan-owned files, 'FILE_BASED' for externally uploaded import sources.",
                    "example": "NATIVE_ANAPLAN"
                  },
                  "separator": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidFilesByFileidChunks": {
      "description": "Minimum role: Standard User.\n\nModel-direct equivalent of `GET /workspaces/{workspaceId}/models/{modelId}/files/{fileId}/chunks`. Returns the same response shape: a `chunks` array of `{id, name}` objects (key omitted entirely when `chunkCount` is 0).",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "fileId",
          "in": "path",
          "description": "File identifier (12-digit numeric, prefix 113). From `GET /models/{modelId}/files`, or the `importDataSourceId` of `GET /workspaces/{workspaceId}/models/{modelId}/imports/` \u2014 the file an import reads, which is the file you upload before running that import.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "chunks": {
              "type": "array",
              "items": {
                "description": "A unit of partial file content",
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidImports": {
      "description": "Minimum role: Standard User.\n\nModel-direct form of `GET /workspaces/{workspaceId}/models/{modelId}/imports`. Returns the same response shape without requiring a workspace ID. Lists import definitions available for the specified model.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort results. `+field` or unprefixed ascending, `-field` descending. Comma-separate for multiple columns. `name` and `id` reorder results. Field name not validated \u2014 unknown fields return 200 and fall back to default order. Example: `+name`, `-id`.",
          "schema": {
            "type": "string"
          },
          "example": "+name"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "imports": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string",
                    "description": "12-digit numeric identifier. The prefix 112 marks this as an import definition."
                  },
                  "importDataSource": {
                    "properties": {
                      "importDataSourceId": {
                        "type": "string"
                      },
                      "sourceModelId": {
                        "type": "string"
                      },
                      "sourceModelName": {
                        "type": "string"
                      },
                      "sourceWorkspaceId": {
                        "type": "string"
                      },
                      "type": {
                        "type": "string"
                      }
                    },
                    "type": "object"
                  },
                  "importDataSourceId": {
                    "type": "string",
                    "description": "The id of the data source this import reads. When the source is an uploaded file this is its file id (prefix 113, from `GET /models/{modelId}/files`). Upload to this file id, then run the import. For model, list, or saved-view sources it is the source object id instead.",
                    "example": "113000000006"
                  },
                  "importType": {
                    "type": "string",
                    "description": "Category of data the import processes.",
                    "enum": [
                      "HIERARCHY_DATA",
                      "LINE_ITEM_DEFINITION",
                      "MODULE_DATA",
                      "USERS"
                    ]
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidImportsByImportid": {
      "description": "Minimum role: Standard User.\n\nModel-direct form of `GET /workspaces/{workspaceId}/models/{modelId}/imports/{importId}`. Returns the same `importMetadata` response shape without requiring a workspace ID.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "importId",
          "in": "path",
          "description": "Import definition identifier (12-digit numeric, prefix 112). From `GET /models/{modelId}/imports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "importMetadata": {
              "description": "Metadata about an import definition",
              "properties": {
                "name": {
                  "type": "string",
                  "description": "Display name of the import definition"
                },
                "type": {
                  "type": "string",
                  "description": "Data source type for this import: 'FILE' for file-based imports, 'MODEL' for model-to-model imports.",
                  "enum": [
                    "FILE",
                    "MODEL"
                  ]
                },
                "source": {
                  "type": "object",
                  "description": "Source file configuration for file-based imports",
                  "properties": {
                    "columnCount": {
                      "type": "integer"
                    },
                    "columnSeparator": {
                      "type": "string"
                    },
                    "decimalSeparator": {
                      "type": "string"
                    },
                    "firstDataRow": {
                      "type": "integer"
                    },
                    "headerNames": {
                      "type": "array",
                      "items": {
                        "type": "string"
                      }
                    },
                    "headerRow": {
                      "type": "integer"
                    },
                    "textDelimiter": {
                      "type": "string"
                    },
                    "textEncoding": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidLineitems": {
      "description": "Minimum role: Standard User.\n\nReturns the identifiers of all line items in a model.\n\n**Notes**:\n\n*   The returned line items are restricted just to those in modules to which the model role of the user has Read access.\n*   The response returns Items within a [flat list](https://help.anaplan.com/anapedia/Content/Modeling/Dimensions/Lists.html) (no hierarchy).\n*   This returns all the line items in the model. For line items in a specific module see Retrieve Line Items For a Module.\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "items": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "appliesTo": {
                    "type": "array",
                    "description": "Lists and list subsets the line item is dimensioned by.",
                    "items": {
                      "type": "object",
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "name": {
                          "type": "string"
                        }
                      }
                    }
                  },
                  "breakback": {
                    "type": "boolean",
                    "description": "Whether breakback is enabled, allowing edits to an aggregate to distribute down to its details."
                  },
                  "broughtForward": {
                    "type": "boolean"
                  },
                  "cellCount": {
                    "type": "integer",
                    "description": "Number of cells the line item occupies across all its dimensions."
                  },
                  "code": {
                    "type": "string"
                  },
                  "dataTags": {
                    "type": "array",
                    "description": "Data tags applied to the line item.",
                    "items": {
                      "type": "object",
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "name": {
                          "type": "string"
                        }
                      }
                    }
                  },
                  "format": {
                    "type": "string",
                    "description": "Data format of the line item (for example, NUMBER, TEXT, BOOLEAN, DATE)."
                  },
                  "formatMetadata": {
                    "type": "object",
                    "description": "Formatting configuration for the line item. The fields present depend on the format.",
                    "properties": {
                      "comparisonIncrease": {
                        "type": "string"
                      },
                      "dataType": {
                        "type": "string"
                      },
                      "decimalPlaces": {
                        "type": "integer"
                      },
                      "decimalSeparator": {
                        "type": "string"
                      },
                      "groupingSeparator": {
                        "type": "string"
                      },
                      "minimumSignificantDigits": {
                        "type": "integer"
                      },
                      "negativeNumberNotation": {
                        "type": "string"
                      },
                      "unitsDisplayType": {
                        "type": "string"
                      },
                      "unitsType": {
                        "type": "string"
                      },
                      "zeroFormat": {
                        "type": "string"
                      }
                    }
                  },
                  "formula": {
                    "type": "string"
                  },
                  "formulaScope": {
                    "type": "string",
                    "description": "Scope the formula applies to (for example, All Versions)."
                  },
                  "id": {
                    "type": "string",
                    "description": "12-digit numeric identifier. The prefix 206 marks this as a line item."
                  },
                  "isSummary": {
                    "type": "boolean",
                    "description": "Whether the line item is a summary (calculated) line item rather than an input."
                  },
                  "moduleId": {
                    "type": "string"
                  },
                  "moduleName": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  },
                  "notes": {
                    "type": "string"
                  },
                  "parent": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "name": {
                        "type": "string"
                      }
                    },
                    "description": "The parent line item, for line items nested under another."
                  },
                  "readAccessDriver": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "name": {
                        "type": "string"
                      }
                    },
                    "description": "Line item that drives read access (cell-level access driver), when configured."
                  },
                  "referencedBy": {
                    "type": "array",
                    "description": "Line items that reference this line item in their formulas.",
                    "items": {
                      "type": "object",
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "name": {
                          "type": "string"
                        }
                      }
                    }
                  },
                  "startOfSection": {
                    "type": "boolean",
                    "description": "Whether this line item starts a new section in the module."
                  },
                  "style": {
                    "type": "string"
                  },
                  "summary": {
                    "type": "string",
                    "description": "Summary method used to aggregate the line item (for example, Sum, None)."
                  },
                  "timeRange": {
                    "type": "string"
                  },
                  "timeScale": {
                    "type": "string"
                  },
                  "useSwitchover": {
                    "type": "boolean",
                    "description": "Whether the line item uses the model's actual-to-forecast switchover."
                  },
                  "version": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "name": {
                        "type": "string"
                      }
                    },
                    "description": "The version the line item is scoped to, when version-specific."
                  },
                  "writeAccessDriver": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "name": {
                        "type": "string"
                      }
                    },
                    "description": "Line item that drives write access (cell-level access driver), when configured."
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidLineitemsByLineitemidDimensions": {
      "description": "Minimum role: Standard User.\n\nReturns the IDs of the dimensions that define a line item.\n\nFor reference, see Retrieve dimension items for a line item.\n\nNotes:\n\n*   This returns a list of all the dimensions that apply to that line item.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "lineItemId",
          "in": "path",
          "description": "Line item identifier (12-digit numeric, prefix 206). From `GET /models/{modelId}/lineItems` or `GET /models/{modelId}/modules/{moduleId}/lineItems`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "dimensions": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidLineitemsByLineitemidDimensionsByDimensionidItems": {
      "description": "Minimum role: Standard User.\n\nReturns the IDs, codes, and names of the dimension items that apply to a line item.\n\n**Notes**:\n\n*   This call works with any dimension type.\n*   Your requesting model role must allow read access to the module the line item belongs to. Otherwise, this call returns a `404 Not Found` HTTP status.\n*   The items in the response are ordered as listed in the Anaplan model.\n*   The response returns items within a [flat list](https://help.anaplan.com/anapedia/Content/Modeling/Dimensions/Lists.html) (no hierarchy).\n*   This returns dimension items that apply to a line item. For the view-level call, see Retrieve selected items in a dimension and for the model-level call, see Retrieve all items in a dimension.\n*   This call only supports the retrieval of dimension items where the dimension contains a maximum of 1,000,000 items. If the specified dimension contains more than 1,000,000 dimension items, then the call returns a 400 Bad Request HTTP status, instead of a subset of the dimension items.\n*   Providing a non-existent Line Item ID or Dimension ID returns a 404 Not Found HTTP status.\n*   If the request provides an invalid line item ID or dimension ID this call returns a `400 Bad Request` HTTP status.\n*   In the future, use of this call may be rate limited.\n*   When the dimension has no items, the response omits the `items` key entirely rather than returning an empty array.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "lineItemId",
          "in": "path",
          "description": "Line item identifier (12-digit numeric, prefix 206). From `GET /models/{modelId}/lineItems` or `GET /models/{modelId}/modules/{moduleId}/lineItems`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "dimensionId",
          "in": "path",
          "description": "Dimension identifier. From `GET /models/{modelId}/lineItems/{lineItemId}/dimensions`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "items": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidLists": {
      "description": "Minimum role: Standard User.\n\nAlternate URL form \u2014 response is equivalent to `/workspaces/{workspaceId}/models/{modelId}/lists`.\n\nReturns the lists in a model.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "lists": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string",
                    "description": "12-digit numeric identifier. The prefix 101 marks this as a list."
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidListsByListid": {
      "description": "Minimum role: Standard User.\n\nAlternate URL form \u2014 response is equivalent to `/workspaces/{workspaceId}/models/{modelId}/lists/{listId}`.\n\nReturns the metadata of a list. Field-level descriptions are in the response schema (ListMetadata). Metadata entries that are not set for a list (for example, no parent or no top level item) are omitted from the response.\n\n**Notes**:\n\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "listId",
          "in": "path",
          "description": "List identifier (12-digit numeric string, prefix 101). From `GET /models/{modelId}/lists`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "metadata": {
              "type": "object",
              "description": "Full metadata for a single list, returned by GET .../lists/{listId}. Metadata entries that are not set for a list (for example, no parent or no top level item) are omitted from the response.",
              "properties": {
                "category": {
                  "type": "string",
                  "description": "The category to which the list is assigned."
                },
                "dataTags": {
                  "type": "array",
                  "description": "The data tags associated with the list.",
                  "items": {
                    "type": "string"
                  }
                },
                "hasSelectiveAccess": {
                  "type": "boolean",
                  "description": "true if this list uses selective access."
                },
                "id": {
                  "type": "string",
                  "description": "The list ID. Corresponds to the listId path parameter. 12-digit numeric, prefix 101."
                },
                "itemCount": {
                  "type": "integer",
                  "description": "The number of list items present in the list."
                },
                "managedBy": {
                  "type": "string",
                  "description": "Not currently used."
                },
                "name": {
                  "type": "string",
                  "description": "The list name."
                },
                "nextitemIndex": {
                  "type": "integer",
                  "description": "Index of the next new item in the list. Used only by numbered lists."
                },
                "numberedList": {
                  "type": "boolean",
                  "description": "true if this is a numbered list."
                },
                "parent": {
                  "type": "object",
                  "description": "The parent hierarchy list, when this list is part of a hierarchy.",
                  "properties": {
                    "id": {
                      "type": "string"
                    },
                    "name": {
                      "type": "string"
                    }
                  }
                },
                "permittedItems": {
                  "type": "integer",
                  "description": "How many more items you can add to the list. Changes as the number of items in the list changes."
                },
                "productionData": {
                  "type": "boolean",
                  "description": "true if this is a production list in Application Lifecycle Management (ALM)."
                },
                "properties": {
                  "type": "array",
                  "description": "The list properties.",
                  "items": {
                    "type": "object",
                    "properties": {
                      "name": {
                        "type": "string"
                      },
                      "dataTags": {
                        "type": "array",
                        "items": {
                          "type": "string"
                        }
                      },
                      "format": {
                        "type": "string"
                      },
                      "formula": {
                        "type": "string"
                      },
                      "notes": {
                        "type": "string"
                      },
                      "referencedBy": {
                        "type": "string"
                      }
                    }
                  }
                },
                "subsets": {
                  "type": "array",
                  "description": "The subsets associated with the list hierarchy.",
                  "items": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "name": {
                        "type": "string"
                      }
                    }
                  }
                },
                "topLevelItem": {
                  "type": "object",
                  "description": "If present, the top level item in a hierarchy.",
                  "properties": {
                    "id": {
                      "type": "string"
                    },
                    "name": {
                      "type": "string"
                    }
                  }
                },
                "useTopLevelAsPageDefault": {
                  "type": "boolean",
                  "description": "true if this list is the default item in a page selector."
                },
                "workflowEnabled": {
                  "type": "boolean",
                  "description": "true if this list is enabled for Workflow."
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidModelcalendar": {
      "description": "Minimum role: Workspace Administrator.\n\nModel-direct equivalent of `GET /workspaces/{workspaceId}/models/{modelId}/modelCalendar`. Returns the same response shape: a `modelCalendar` object containing the current fiscal year with start and end dates (empty object when the calendar type has no fiscal year, e.g., Weeks General).",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "modelCalendar": {
              "properties": {
                "calendarType": {
                  "type": "string",
                  "description": "Calendar model used in this model. Determines the period hierarchy and week-based options.",
                  "example": "Calendar Months"
                },
                "currentPeriod": {
                  "properties": {
                    "lastDay": {
                      "type": "string"
                    },
                    "periodText": {
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "extraWeekMonth": {
                  "type": "integer",
                  "description": "For 4-4-5 calendars, the month index (1\u201312) that receives the extra week in a 53-week year."
                },
                "fiscalYear": {
                  "properties": {
                    "endDate": {
                      "type": "string"
                    },
                    "startDate": {
                      "type": "string"
                    },
                    "year": {
                      "type": "string",
                      "description": "The current fiscal year label, in the model's calendar format (for example, FY21).",
                      "example": "FY21"
                    }
                  },
                  "type": "object"
                },
                "futureYearsCount": {
                  "type": "integer"
                },
                "pastYearsCount": {
                  "type": "integer"
                },
                "startDate": {
                  "type": "string"
                },
                "totalsSelection": {
                  "properties": {
                    "extraMonthQuarter": {
                      "type": "integer"
                    },
                    "isHalfYearTotals": {
                      "type": "boolean"
                    },
                    "isQuarterTotals": {
                      "type": "boolean"
                    },
                    "isTotalOfAllPeriods": {
                      "type": "boolean"
                    },
                    "isYearToDateSummary": {
                      "type": "boolean"
                    },
                    "isYearToGoSummary": {
                      "type": "boolean"
                    }
                  },
                  "type": "object"
                },
                "weekFormat": {
                  "type": "string"
                },
                "weekGrouping": {
                  "type": "string"
                },
                "weeksCount": {
                  "type": "integer"
                }
              }
            }
          }
        }
      }
    },
    "putModelsByModelidModelcalendarFiscalyear": {
      "description": "Minimum role: Workspace Administrator.\n\nUpdates the current fiscal year of the model calendar. The year input value has to be a valid fiscal year supported by the model calendar in Anaplan. If successful, this call returns the updated fiscal year with start and end date associated with current model calendar type. This call returns a `400 bad request` message if the input value is out of range or model calendar type does not have an available fiscal year (for example, Weeks General).",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": {
        "media_type": "application/json",
        "required": false,
        "schema": {
          "type": "object",
          "properties": {
            "year": {
              "type": "string",
              "description": "Fiscal year to set as current. Must be a valid fiscal year for the model's calendar type (for example, FY21)."
            }
          }
        }
      },
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "modelCalendar": {
              "properties": {
                "calendarType": {
                  "type": "string",
                  "description": "Calendar model used in this model. Determines the period hierarchy and week-based options.",
                  "example": "Calendar Months"
                },
                "currentPeriod": {
                  "properties": {
                    "lastDay": {
                      "type": "string"
                    },
                    "periodText": {
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "extraWeekMonth": {
                  "type": "integer",
                  "description": "For 4-4-5 calendars, the month index (1\u201312) that receives the extra week in a 53-week year."
                },
                "fiscalYear": {
                  "properties": {
                    "endDate": {
                      "type": "string"
                    },
                    "startDate": {
                      "type": "string"
                    },
                    "year": {
                      "type": "string",
                      "description": "The current fiscal year label, in the model's calendar format (for example, FY21).",
                      "example": "FY21"
                    }
                  },
                  "type": "object"
                },
                "futureYearsCount": {
                  "type": "integer"
                },
                "pastYearsCount": {
                  "type": "integer"
                },
                "startDate": {
                  "type": "string"
                },
                "totalsSelection": {
                  "properties": {
                    "extraMonthQuarter": {
                      "type": "integer"
                    },
                    "isHalfYearTotals": {
                      "type": "boolean"
                    },
                    "isQuarterTotals": {
                      "type": "boolean"
                    },
                    "isTotalOfAllPeriods": {
                      "type": "boolean"
                    },
                    "isYearToDateSummary": {
                      "type": "boolean"
                    },
                    "isYearToGoSummary": {
                      "type": "boolean"
                    }
                  },
                  "type": "object"
                },
                "weekFormat": {
                  "type": "string"
                },
                "weekGrouping": {
                  "type": "string"
                },
                "weeksCount": {
                  "type": "integer"
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidModules": {
      "description": "Minimum role: Standard User.\n\nReturns the IDs and names of all modules in a model.\n\n**Notes**:\n\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "modules": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string",
                    "description": "12-digit numeric identifier. The prefix 102 marks this as a module."
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "postModelsByModelidModulesByModuleidData": {
      "description": "Minimum role: Standard User.\n\nSets cell values in a module.\n\nEnsure the request body is an array of `CellWrite` objects. Each object is for an updated individual cell and its full set of dimensions.\n\nThe response contains a summary of the number of cells updated successfully and the details of why some cells that could not be updated.\n\n**Notes:**\n\n*   Ensure the user has write access enabled.\n*   Ensure the request includes all of the required fields.\n*   Ensure the request has a `value` field that is either a string, number or boolean and is compatible with the line item format/data type.\n*   If one cell cannot update, the other cells in the request still continue to update.\n*   Workspace administrators can import data into any cell (except aggregate cells, see below), including read-only and invisible cells.\n*   You can update a maximum of 1000 cells per request. For larger scale writes we recommend you use the import API.\n*   Write cell data does not apply to aggregate cells.\n    *   An aggregate cell is a cell where one of its dimension items has it\u2019s own child items\n    *   This includes aggregate cells for line Items with breakback enabled\n    *   For a line Item that is dimensioned by a composite list, the only writeable cells are the child items, that are defined directly in the dimension list and not defined in a parent list.\n*   In the future, use of this call may be rate limited.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "moduleId",
          "in": "path",
          "description": "Module identifier (12-digit numeric string, prefix 102). From `GET /models/{modelId}/modules`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": {
        "media_type": "application/json",
        "required": true,
        "schema": {
          "type": "array",
          "items": {
            "type": "object",
            "description": "A request to set the value of a single cell, identified by the line item it belongs to plus one coordinate for each dimension that applies to that line item (including Time and Versions).",
            "properties": {
              "lineItemId": {
                "type": "string",
                "description": "ID of the line item that belongs to the module. Specify either lineItemId or lineItemName. From `GET /models/{modelId}/modules/{moduleId}/lineItems` (`items[].id`)."
              },
              "lineItemName": {
                "type": "string",
                "description": "Name of the line item that belongs to the module. Specify either lineItemId or lineItemName."
              },
              "dimensions": {
                "type": "array",
                "description": "Coordinate for each dimension that applies to the line item. May be omitted when the line item is a single cell with no dimensions.",
                "items": {
                  "type": "object",
                  "description": "A dimension coordinate: the dimension plus the item selected on it.",
                  "properties": {
                    "dimensionId": {
                      "type": "string",
                      "description": "ID of the dimension on the line item. Specify either dimensionId or dimensionName. From `GET /models/{modelId}/lineItems/{lineItemId}/dimensions` (`dimensions[].id`)."
                    },
                    "dimensionName": {
                      "type": "string",
                      "description": "Name of the dimension on the line item. Specify either dimensionId or dimensionName."
                    },
                    "itemId": {
                      "type": "string",
                      "description": "ID of the item selected on the dimension. Specify one of itemId, itemName, or itemCode. From `GET /models/{modelId}/dimensions/{dimensionId}/items` (`items[].id`)."
                    },
                    "itemName": {
                      "type": "string",
                      "description": "Name of the item selected on the dimension. Specify one of itemId, itemName, or itemCode."
                    },
                    "itemCode": {
                      "type": "string",
                      "description": "Code of the item selected on the dimension. Specify one of itemId, itemName, or itemCode."
                    }
                  }
                }
              },
              "value": {
                "description": "The value to set for the cell. Must be a string, number, or boolean compatible with the line item's format/data type. Cannot be null.",
                "oneOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "number"
                  },
                  {
                    "type": "boolean"
                  }
                ]
              }
            },
            "required": [
              "value"
            ]
          }
        }
      },
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "numberOfCellsChanged": {
              "type": "number"
            },
            "failures": {
              "description": "An array of cell write failures. The array is empty when all cells update successfully.",
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "requestIndex": {
                    "description": "Index of the cell write request from the request array.",
                    "type": "number"
                  },
                  "failureType": {
                    "description": "Failure reason.",
                    "type": "string"
                  },
                  "failureMessageDetails": {
                    "description": "Details on the failure reason.",
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidModulesByModuleidLineitems": {
      "description": "Minimum role: Standard User.\n\n**Note:**This is an early access endpoint and is not generally available yet.\n\nUse this call when you want to get all line items and metadata in a module.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "moduleId",
          "in": "path",
          "description": "Module identifier (12-digit numeric string, prefix 102). From `GET /models/{modelId}/modules`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "includeAll",
          "in": "query",
          "description": "When `true`, optional fields are included in the response (for line items, the full metadata field set). When `false` or omitted, only the default fields are returned.",
          "required": false,
          "schema": {
            "type": "boolean"
          },
          "example": true
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "items": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "appliesTo": {
                    "type": "array",
                    "description": "Lists and list subsets the line item is dimensioned by.",
                    "items": {
                      "type": "object",
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "name": {
                          "type": "string"
                        }
                      }
                    }
                  },
                  "breakback": {
                    "type": "boolean",
                    "description": "Whether breakback is enabled, allowing edits to an aggregate to distribute down to its details."
                  },
                  "broughtForward": {
                    "type": "boolean"
                  },
                  "cellCount": {
                    "type": "integer",
                    "description": "Number of cells the line item occupies across all its dimensions."
                  },
                  "code": {
                    "type": "string"
                  },
                  "dataTags": {
                    "type": "array",
                    "description": "Data tags applied to the line item.",
                    "items": {
                      "type": "object",
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "name": {
                          "type": "string"
                        }
                      }
                    }
                  },
                  "format": {
                    "type": "string",
                    "description": "Data format of the line item (for example, NUMBER, TEXT, BOOLEAN, DATE)."
                  },
                  "formatMetadata": {
                    "type": "object",
                    "description": "Formatting configuration for the line item. The fields present depend on the format.",
                    "properties": {
                      "comparisonIncrease": {
                        "type": "string"
                      },
                      "dataType": {
                        "type": "string"
                      },
                      "decimalPlaces": {
                        "type": "integer"
                      },
                      "decimalSeparator": {
                        "type": "string"
                      },
                      "groupingSeparator": {
                        "type": "string"
                      },
                      "minimumSignificantDigits": {
                        "type": "integer"
                      },
                      "negativeNumberNotation": {
                        "type": "string"
                      },
                      "unitsDisplayType": {
                        "type": "string"
                      },
                      "unitsType": {
                        "type": "string"
                      },
                      "zeroFormat": {
                        "type": "string"
                      }
                    }
                  },
                  "formula": {
                    "type": "string"
                  },
                  "formulaScope": {
                    "type": "string",
                    "description": "Scope the formula applies to (for example, All Versions)."
                  },
                  "id": {
                    "type": "string",
                    "description": "12-digit numeric identifier. The prefix 206 marks this as a line item."
                  },
                  "isSummary": {
                    "type": "boolean",
                    "description": "Whether the line item is a summary (calculated) line item rather than an input."
                  },
                  "moduleId": {
                    "type": "string"
                  },
                  "moduleName": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  },
                  "notes": {
                    "type": "string"
                  },
                  "parent": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "name": {
                        "type": "string"
                      }
                    },
                    "description": "The parent line item, for line items nested under another."
                  },
                  "readAccessDriver": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "name": {
                        "type": "string"
                      }
                    },
                    "description": "Line item that drives read access (cell-level access driver), when configured."
                  },
                  "referencedBy": {
                    "type": "array",
                    "description": "Line items that reference this line item in their formulas.",
                    "items": {
                      "type": "object",
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "name": {
                          "type": "string"
                        }
                      }
                    }
                  },
                  "startOfSection": {
                    "type": "boolean",
                    "description": "Whether this line item starts a new section in the module."
                  },
                  "style": {
                    "type": "string"
                  },
                  "summary": {
                    "type": "string",
                    "description": "Summary method used to aggregate the line item (for example, Sum, None)."
                  },
                  "timeRange": {
                    "type": "string"
                  },
                  "timeScale": {
                    "type": "string"
                  },
                  "useSwitchover": {
                    "type": "boolean",
                    "description": "Whether the line item uses the model's actual-to-forecast switchover."
                  },
                  "version": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "name": {
                        "type": "string"
                      }
                    },
                    "description": "The version the line item is scoped to, when version-specific."
                  },
                  "writeAccessDriver": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "name": {
                        "type": "string"
                      }
                    },
                    "description": "Line item that drives write access (cell-level access driver), when configured."
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidModulesByModuleidViews": {
      "description": "Minimum role: Standard User.\n\nReturns the IDs and names of all views in a module. This call also returns the default view for the module. If the user adds the query parameter includesubsidiaryviews=true, unsaved subsidiary views will also be included in the results.\n\n**Notes**:\n\n*   For the default view, the `{viewId}` is the same as the `{moduleId}`\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response\n*   For unsaved subsidiary views, the `{viewId}` and `{viewName}` are the same as the `{lineItemId}` and `{lineItemName}` that the subsidiary view is created from",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "moduleId",
          "in": "path",
          "description": "Module identifier (12-digit numeric string, prefix 102). From `GET /models/{modelId}/modules`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "includesubsidiaryviews",
          "in": "query",
          "schema": {
            "type": "boolean"
          },
          "example": true
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "views": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string",
                    "description": "12- or 13-digit numeric identifier. Default views share the module prefix 102. Named saved views use higher-range prefixes (up to 13 digits)."
                  },
                  "moduleId": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidProcesses": {
      "description": "Minimum role: Standard User.\n\nModel-direct form of `GET /workspaces/{workspaceId}/models/{modelId}/processes`. Returns the same response shape without requiring a workspace ID. Lists process definitions available for the specified model.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort results. `+field` or unprefixed ascending, `-field` descending. Comma-separate for multiple columns. `name` and `id` reorder results. Field name not validated \u2014 unknown fields return 200 and fall back to default order. Example: `+name`, `-id`.",
          "schema": {
            "type": "string"
          },
          "example": "+name"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "processes": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string",
                    "description": "12-digit numeric identifier. The prefix 118 marks this as a process."
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidProcessesByProcessid": {
      "description": "Minimum role: Standard User.\n\nReturns the name and the actions of a process in an Anaplan model.\n\n**Notes**:\n\n*   Actions which are imports or exports (`actionType` field is `IMPORT` or `EXPORT`) contain additional fields in the response (see below). Other actions just contain the `id`, `name`, and `actionType`.\n*   Actions are ordered in the sequence that they run when the Process itself is executed.\n*   In the future, use of this call may be rate limited.\n*   For an action with `actionType` of `IMPORT`, you can use the `importDataSourceId` value as the ID of a file for the `fileId` parameter when you upload or download files\n*   For actions with `actionType` of `EXPORT` or `IMPORT`, you can find more information about them if you use the IDs as `exportId` or `importId` in the export and import metadata endpoints.\n*   The possible values for actionType (non-exhaustive list):\n    *   IMPORT\n    *   EXPORT\n    *   DELETE_BY_SELECTION\n    *   ORDER_HIERARCHY\n    *   OPTIMIZER\n*   To include additional information regarding the import data source in an import action, add the `?showImportDataSource=true` query parameter to the request.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "processId",
          "in": "path",
          "description": "Process identifier (12-digit numeric, prefix 118). From `GET /models/{modelId}/processes`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "showImportDataSource",
          "in": "query",
          "description": "When set to \"true\", includes additional information about the import data source. Any value other than \"true\" is interpreted as false.",
          "schema": {
            "type": "string"
          },
          "example": "true"
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "processMetadata": {
              "properties": {
                "code": {
                  "type": "string"
                },
                "id": {
                  "type": "string",
                  "description": "12-digit numeric identifier. The prefix 118 marks this as a process."
                },
                "name": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidVersions": {
      "description": "Minimum role: Workspace Administrator.\n\nReturns information about the model versions. The response contains a list of the versions that the user has access to, and the model metadata that can be viewed from the UI.\n\n**Notes:**\n\n*   The returned versions only include the user\u2019s model role with read access or higher.\n*   If the value for a field is set, it will be returned in the results.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "versionMetadata": {
              "type": "array",
              "items": {
                "description": "The version of data being used in an Anaplan model",
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "editFrom": {
                    "properties": {
                      "date": {
                        "type": "string"
                      },
                      "periodText": {
                        "type": "string"
                      }
                    }
                  },
                  "editTo": {
                    "properties": {
                      "date": {
                        "type": "string"
                      },
                      "periodText": {
                        "type": "string"
                      }
                    }
                  },
                  "formula": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "isActual": {
                    "type": "boolean",
                    "description": "Whether this version holds actual (historical) data rather than forecast data."
                  },
                  "isCurrent": {
                    "type": "boolean",
                    "description": "Whether this is the currently active version, i.e. the version whose data covers the current period."
                  },
                  "name": {
                    "type": "string"
                  },
                  "notes": {
                    "type": "string"
                  },
                  "switchover": {
                    "properties": {
                      "date": {
                        "type": "string"
                      },
                      "periodText": {
                        "type": "string"
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "putModelsByModelidVersionsByVersionidSwitchover": {
      "description": "Minimum role: Workspace Administrator.\n\nSets the switchover date of a version. The response contains the response code, as well as the date and human-readable date period as shown in the UI.\n\n**Notes:**\n\n*   To apply a switchover date, ensure the `versionID` corresponds to a version in Anaplan that is set to either `forecast` or `variance`.\n*   Ensure the switchover date is after the existing version switchover date.\n*   To reset the switchover date to Blank, pass an empty string in the request body.\n*   In the future, use of this call may be rate limited.\n\n### Request body\n\n| **JSON element** | **Details** |\n| --- | --- |\n| `date` | <br><br>*   Required<br>*   Description: An ISO-formatted (YYYY-MM-DD) date string is used to set the switchover date. An empty string resets the switchover date.  <br>    Dates earlier than the current switchover date (for example, `2021-01-01` when the current switchover is `2021-06-01`) or dates outside the current model year return a `400 Bad Request` response.<br>*   Example: `2021-06-01`<br><br> |",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "versionId",
          "in": "path",
          "description": "Version identifier (12-digit numeric, prefix 107). From `GET /models/{modelId}/versions`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": {
        "media_type": "application/json",
        "required": false,
        "schema": {
          "properties": {
            "date": {
              "format": "date",
              "type": "string"
            }
          },
          "type": "object"
        }
      },
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "versionSwitchover": {
              "description": "The date that the version switches over from actual to forecast",
              "properties": {
                "calendarType": {
                  "type": "string"
                },
                "code": {
                  "type": "string"
                },
                "date": {
                  "type": "string"
                },
                "id": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "periodText": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidViews": {
      "description": "Minimum role: Standard User.\n\nAlternate URL form \u2014 response is equivalent to `/workspaces/{workspaceId}/models/{modelId}/views`.\n\nReturns the ID and name of all views in a model. The results include default and saved views. If you add the query parameter `includesubsidiaryviews=true`, this includes unsaved subsidiary views in the results. This call also retrieves the ID of the module that the views belong to.\n\n**Notes**:\n\n*   Single quotes enclose the module and view names that have special characters\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response\n\n### Default views\n\nFor default views:\n\n*   The value of the {viewId} is identical to the value of the {moduleId}\n*   The value of the name is identical to the value of the module\n\n### Saved views\n\nFor saved views:\n\n*   The `name` consists of the module name, a period, and the saved view name. For example, `REP01 Profit & Loss Report.P & L by Country`\n    \n\nFor unsaved subsidiary views:\n\n*   The name consists of the module name and the line item name that the subsidiary view is created from\n*   These are separated by a period(.)",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "includesubsidiaryviews",
          "in": "query",
          "description": "When set to \"true\", includes unsaved subsidiary views in the results. Any value other than \"true\" is interpreted as false.",
          "schema": {
            "type": "string"
          },
          "example": "true"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "views": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string",
                    "description": "12- or 13-digit numeric identifier. Default views share the module prefix 102. Named saved views use higher-range prefixes (up to 13 digits)."
                  },
                  "moduleId": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidViewsByViewid": {
      "description": "Minimum role: Standard User.\n\nReturns the name, the IDs, and the lists of names for the dimensions (columns, pages, rows) on a view.\n\n**Notes**:\n\n*   If a view has no dimensions on an axis, the call omits the axis field (for example, no `\u201crows\u201d` field at all instead of `\u201crows\u201d: []`)\n*   If a view is the default view, its `name` is set to be the same as the module name\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response\n*   The `viewId` in this request can also be replaced by any valid `lineItemId`. If the Line Item has a changed dimensionality (has a Subsidiary View), the response returns the metadata applicable to that line item. Otherwise, the response returns the default metadata\n    \n\nYou can use this API call to [map dimensions to cell data export headers](https://help.anaplan.com/43cd0223-edaa-4289-9833-eb9b73666aac-Map-dimensions-to-cell-data-export-headers) or [transform grid CSV format to tabular single column format](https://help.anaplan.com/c8a665b9-6b23-4254-ac68-816657a6e878-Retrieve-the-view-metadata-and-grid-view-data).",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "viewId",
          "in": "path",
          "description": "View identifier. From `GET /models/{modelId}/views` or `GET /models/{modelId}/modules/{moduleId}/views`. A line item ID from `GET /models/{modelId}/modules/{moduleId}/lineItems` also works when that line item has a subsidiary view, returning that view's data.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "viewId": {
              "type": "string"
            },
            "viewName": {
              "type": "string"
            },
            "rows": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            },
            "columns": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            },
            "pages": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidViewsByViewidData": {
      "description": "Minimum role: Standard User.\n\nReturns the cell data of a view. You can either query for the default page, or provide page selectors to query for other pages. This call returns data in either a CSV grid response or as JSON.\n\n**Notes:**\n\n*   This call only supports the retrieval of cells from a view that contains a maximum of 1,000,000 cells. If the specified view contains more than 1,000,000 cells, then the call returns a `400 Bad Request` HTTP status, instead of a subset of the cells. Use the [quick sum bar](https://help.anaplan.com/anapedia/Content/New_User_Experience/Use/Worksheets/Grid/Quick-sum-bar.htm) in Anaplan to identify the number of cells in a view.\n*   If you call several times in a short amount of time, you may receive a `429` response.\n*   You can replace the `viewId` with any valid `lineItemId`. If the line item has a subsidiary view, the response returns the data as seen in that subsidiary view.\n*   For line items without a subsidiary view, the response returns data for the default view of the line item.\n\n### CSV responses\n\nThe first line of the CSV response contains the values of the page selectors. The response omits this line when there are no dimensions on pages.\n\nWhen there are multiple dimensions on columns, a line for each dimension follows the first line that contains the values of the page selectors.\n\nWhen there are multiple dimensions on rows, a column displays on the left, before the data, for each dimension on rows.\n\n### JSON responses\n\nTo receive a JSON-formatted response instead of a CSV-formatted response:\n\n1.  Pass an `Accept` header of `application/json`.\n2.  Pass a query parameter of `format` set to `v1`.\n    \n\nAll other parameters are the same as a CSV-formatted response.\n\nThe response returns `Content-Type` `application/json` with this format:\n\n*   A `pages` key with a value that is a list of the selected items for each page selector. If no page selectors exist for this view, the list displays as empty or absent.\n*   A `columnCoordinates` key with a value that is a list of the coordinates for all values in that column. Each list item is itself a list containing one or more values. The length of each list equals the number of dimensions on columns.\n*   A `rows` key with a value that is a list of row objects. Each row object contains:\n    *   A `rowCoordinates` key with a value that is a list of the coordinates for all values in the current row. Each list item is itself a list that contains one or more values. The length of each list equals the number of dimensions on rows.\n    *   A `cells` key with a value, which is a list of all the cell values in the current row.\n\nTo get the full coordinates for each cell, use the position of the cell value in the `cells` list, and find the column coordinates at the same position in the `columnCoordinates` list. Then, to uniquely specify the cell, combine the column coordinates with the `rowCoordinates` for the current row and any selected pages.\n\n**HIGH VOLUME** \u2014 a full grid export is returned in one non-paginated response (observed ~210 KB) and there is no `limit`/`offset` paging (the call is instead capped at 1,000,000 cells, returning `400` above that). Narrow the request with page selectors and request only the view/pages you need.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "viewId",
          "in": "path",
          "description": "View identifier. From `GET /models/{modelId}/views` or `GET /models/{modelId}/modules/{moduleId}/views`. A line item ID from `GET /models/{modelId}/modules/{moduleId}/lineItems` also works when that line item has a subsidiary view, returning that view's data.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "Accept",
          "in": "header",
          "description": "Response format. `text/csv` (default) returns a CSV grid. `application/json` returns structured cell data (also requires query parameter format=v1). Append `;escaped=true` to quote sensitive CSV fields. Some tenants block plain text/csv for security and require the escaped form.",
          "required": false,
          "schema": {
            "type": "string",
            "enum": [
              "text/csv",
              "text/csv;escaped=true",
              "application/json"
            ],
            "default": "text/csv"
          }
        },
        {
          "name": "format",
          "in": "query",
          "description": "Set to `v1` together with Accept: application/json to receive JSON. Ignored for CSV responses.",
          "schema": {
            "type": "string",
            "enum": [
              "v1"
            ]
          },
          "example": "v1"
        },
        {
          "name": "pages",
          "in": "query",
          "description": "Page selector values, each in the form `dimensionId:itemId`, choosing which item to display for each page-axis dimension. The page-axis `dimensionId` values come from the `pages` array of `GET /models/{modelId}/views/{viewId}`. The `itemId` values for a given dimension come from `GET /workspaces/{workspaceId}/models/{modelId}/dimensions/{dimensionId}/items`. Both repeated-key form (`pages=a:b&pages=c:d`) and comma-separated form (`pages=a:b,c:d`) are accepted.",
          "required": false,
          "schema": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "example": [
            "101000000026:330000000028",
            "101000000020:330000000028"
          ],
          "style": "form",
          "explode": true
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "description": "JSON-formatted view cell data returned when Accept: application/json and format=v1 are set on GET /models/{modelId}/views/{viewId}/data.",
          "type": "object",
          "properties": {
            "pages": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Selected item labels for each page-axis dimension. The array is empty when no page dimensions exist."
            },
            "columnCoordinates": {
              "type": "array",
              "items": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "description": "Coordinate labels for each column. Each inner array has one entry per column dimension."
            },
            "rows": {
              "type": "array",
              "items": {
                "description": "One row of view cell data.",
                "type": "object",
                "properties": {
                  "rowCoordinates": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Coordinate labels for this row. There is one entry per row dimension."
                  },
                  "cells": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Cell values in column order. Positions correspond to columnCoordinates."
                  }
                }
              }
            }
          }
        }
      }
    },
    "getModelsByModelidViewsByViewidDimensionsByDimensionidItems": {
      "description": "Minimum role: Standard User.\n\nReturns the selected IDs, codes, and names of the items in a dimension. This call returns data as filtered by the page builder when they configure the view. This call respects hidden items, filtering selections, and [Selective Access](https://help.anaplan.com/anapedia/Content/Modeling/Users/Selective_Access.html). If the view contains hidden or filtered items, these do not display in the response.\n\n**Notes**:\n\n*   This call works with any dimension type\n*   The items in the response may not be ordered\n*   The response returns Items within a [flat list](https://help.anaplan.com/anapedia/Content/Modeling/Dimensions/Lists.html) (no hierarchy)\n*   This is a view-level call. For the model-level call, see Retrieve all data for items in a dimension\n*   This call only supports the retrieval of dimension items within a view that contains a maximum of 1,000,000 cells. If the specified view contains more than 1,000,000 dimension items, then the call returns a `400 Bad Request` HTTP status, instead of a subset of the dimension items. Use the [quick sum bar](https://help.anaplan.com/anapedia/Content/New_User_Experience/Use/Worksheets/Grid/Quick-sum-bar.htm) in Anaplan to identify the number of dimension items in a view\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response\n*   The `viewId` in this request can also be replaced by any valid `lineItemId`. If the Line Item has a changed dimensionality (has a Subsidiary View), the response returns the applicable dimension items for the given dimension. Otherwise, the response returns all default applicable dimension items for the given dimension\n\n**Note**: When the dimension has no items, the response omits the `items` key entirely rather than returning an empty array.\n\n**HIGH VOLUME** \u2014 all selected items are returned in one non-paginated response (observed ~149 KB). There is no `limit`/`offset` paging (the call is instead capped at 1,000,000 cells, returning `400` above that). Scope to the specific dimension and view you need rather than requesting broad dimensions.",
      "parameters": [
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "viewId",
          "in": "path",
          "description": "View identifier. From `GET /models/{modelId}/views` or `GET /models/{modelId}/modules/{moduleId}/views`. A line item ID from `GET /models/{modelId}/modules/{moduleId}/lineItems` also works when that line item has a subsidiary view, returning that view's data.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "dimensionId",
          "in": "path",
          "description": "Dimension identifier. From `GET /models/{modelId}/lineItems/{lineItemId}/dimensions`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "items": {
              "type": "array",
              "items": {
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getUsers": {
      "description": "Minimum role: Standard User.\n\nRetrieves all users within the tenant.\n\nDeactivated users (`active` is `false`) are typically not relevant to callers listing people. Filter to `active=true` unless you specifically need inactive users.\n\n**HIGH VOLUME** \u2014 this returns every user in the tenant in one merged response. With no `limit` a client that auto-follows `meta.paging` combines every page and can overflow its token budget. Reduce volume by passing an explicit `limit` and paging with `offset`, by scoping server-side with the `s=` search filter instead of full enumeration, and/or by using `search` to locate the specific op/params rather than pulling the whole collection.",
      "parameters": [
        {
          "name": "s",
          "in": "query",
          "description": "Optional case-insensitive substring filter (matches anywhere in the field). Supports wildcards `%` (0-n characters) and `_` (exactly 1 character). Omit to return all items. **Undocumented, and behavior is endpoint-specific.** `GET /workspaces` and `GET /users` are verified with the Workspace Administrator role. `GET /models` requires the Tenant Admin role. Without that role it returns the full model list unfiltered.",
          "required": false,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort results. `+field` ascending, `-field` descending. The prefix is required and the field name is validated (bad or unknown field returns 400). Comma-separate to sort by multiple columns. Sortable fields: firstName, lastName, email, active, lastLoginDate.",
          "schema": {
            "type": "string"
          },
          "example": "+firstName"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "users": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string",
                    "pattern": "^[0-9a-fA-F]{32}$"
                  },
                  "firstName": {
                    "type": "string"
                  },
                  "lastName": {
                    "type": "string"
                  },
                  "email": {
                    "type": "string",
                    "pattern": "^[_A-Za-z0-9-\\\\+]+(\\\\.[_A-Za-z0-9-]+)*@[A-Za-z0-9-]+(\\\\.[A-Za-z0-9]+)*(\\\\.[A-Za-z]{2,})$"
                  },
                  "active": {
                    "type": "boolean"
                  },
                  "lastLoginDate": {
                    "type": "string",
                    "format": "date-time"
                  },
                  "emailOptIn": {
                    "type": "boolean"
                  },
                  "customerId": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getUsersMe": {
      "description": "Minimum role: Standard User.\n\nRetrieves your user based on your Anaplan authentication token.",
      "parameters": [],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "user": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "pattern": "^[0-9a-fA-F]{32}$"
                },
                "firstName": {
                  "type": "string"
                },
                "lastName": {
                  "type": "string"
                },
                "email": {
                  "type": "string",
                  "pattern": "^[_A-Za-z0-9-\\\\+]+(\\\\.[_A-Za-z0-9-]+)*@[A-Za-z0-9-]+(\\\\.[A-Za-z0-9]+)*(\\\\.[A-Za-z]{2,})$"
                },
                "active": {
                  "type": "boolean"
                },
                "lastLoginDate": {
                  "type": "string",
                  "format": "date-time"
                },
                "emailOptIn": {
                  "type": "boolean"
                },
                "customerId": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "getUsersByUserid": {
      "description": "Minimum role: Standard User.\n\nRetrieves information about the specified user.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`",
      "parameters": [
        {
          "name": "userId",
          "in": "path",
          "description": "User identifier (32-character hex string). From `GET /users`, or `GET /users/me` for the caller's own id.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "user": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "pattern": "^[0-9a-fA-F]{32}$"
                },
                "firstName": {
                  "type": "string"
                },
                "lastName": {
                  "type": "string"
                },
                "email": {
                  "type": "string",
                  "pattern": "^[_A-Za-z0-9-\\\\+]+(\\\\.[_A-Za-z0-9-]+)*@[A-Za-z0-9-]+(\\\\.[A-Za-z0-9]+)*(\\\\.[A-Za-z]{2,})$"
                },
                "active": {
                  "type": "boolean"
                },
                "lastLoginDate": {
                  "type": "string",
                  "format": "date-time"
                },
                "emailOptIn": {
                  "type": "boolean"
                },
                "customerId": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "getWorkspaces": {
      "description": "Minimum role: Standard User.\n\nRetrieves all workspaces a user has access to within the user's default tenant\n\n**HIGH VOLUME** \u2014 this returns every workspace the user can access in one merged response. With no `limit` a client that auto-follows `meta.paging` combines every page and can overflow its token budget (a `getWorkspaces` \u2192 per-workspace admins pipe produced a 335 KB result that exceeded the token cap). Reduce volume by passing an explicit `limit` and paging with `offset`, by scoping server-side with the `s=` search filter instead of full enumeration, and/or by using `search` to locate the specific op/params rather than pulling the whole collection.",
      "parameters": [
        {
          "name": "tenantDetails",
          "in": "query",
          "description": "When `true`, the response includes additional workspace detail fields (`sizeAllowance` and `currentSize`). When `false` or omitted, only `id`, `name`, and `active` are returned.",
          "required": false,
          "schema": {
            "type": "boolean"
          },
          "example": true
        },
        {
          "name": "s",
          "in": "query",
          "description": "Optional case-insensitive substring filter (matches anywhere in the field). Supports wildcards `%` (0-n characters) and `_` (exactly 1 character). Omit to return all items. **Undocumented, and behavior is endpoint-specific.** `GET /workspaces` and `GET /users` are verified with the Workspace Administrator role. `GET /models` requires the Tenant Admin role. Without that role it returns the full model list unfiltered.",
          "required": false,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort results. `+field` ascending, `-field` descending. The prefix is required and the field name is validated (bad or unknown field returns 400). Comma-separate to sort by multiple columns. Sortable fields: name, sizeAllowance.",
          "schema": {
            "type": "string"
          },
          "example": "+name"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "workspaces": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string",
                    "pattern": "^[0-9a-fA-F]{32}$"
                  },
                  "name": {
                    "type": "string"
                  },
                  "active": {
                    "type": "boolean"
                  },
                  "sizeAllowance": {
                    "type": "integer"
                  },
                  "currentSize": {
                    "type": "integer"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceid": {
      "description": "Minimum role: Standard User.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "tenantDetails",
          "in": "query",
          "description": "When `true`, the response includes additional workspace detail fields (`sizeAllowance` and `currentSize`). When `false` or omitted, only `id`, `name`, and `active` are returned.",
          "required": false,
          "schema": {
            "type": "boolean"
          },
          "example": true
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "workspace": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "pattern": "^[0-9a-fA-F]{32}$"
                },
                "name": {
                  "type": "string"
                },
                "active": {
                  "type": "boolean"
                },
                "sizeAllowance": {
                  "type": "integer"
                },
                "currentSize": {
                  "type": "integer"
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidAdmins": {
      "description": "Minimum role: User Administrator.\n\nReturns all administrators for a workspace. Requires tenant admin privilege. Workspace admin access alone is insufficient.\n\nDeactivated administrators (`active` is `false`) are typically not relevant to callers listing people. Filter to `active=true` unless you specifically need inactive administrators.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "admins": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "firstName": {
                    "type": "string"
                  },
                  "lastName": {
                    "type": "string"
                  },
                  "email": {
                    "type": "string",
                    "pattern": "^[_A-Za-z0-9-\\\\+]+(\\\\.[_A-Za-z0-9-]+)*@[A-Za-z0-9-]+(\\\\.[A-Za-z0-9]+)*(\\\\.[A-Za-z]{2,})$"
                  },
                  "active": {
                    "type": "boolean"
                  },
                  "otherWorkspaces": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "postWorkspacesByWorkspaceidBulkdeletemodels": {
      "description": "Minimum role: Standard User.\n\nDeletes multiple models in a workspace in a single request. Models that cannot be deleted (e.g., because they are currently open) are listed in `bulkDeleteModelsFailures`. The overall HTTP status is still 200 in that case.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": {
        "media_type": "application/json",
        "required": true,
        "schema": {
          "type": "object",
          "required": [
            "modelIdsToDelete"
          ],
          "properties": {
            "modelIdsToDelete": {
              "type": "array",
              "description": "IDs of the models to delete.",
              "items": {
                "type": "string"
              }
            }
          }
        }
      },
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "description": "Result of bulk deleting models.",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "modelsDeleted": {
              "type": "integer",
              "description": "Number of models successfully deleted."
            },
            "bulkDeleteModelsFailures": {
              "type": "array",
              "description": "Models that could not be deleted, with failure reasons. Omitted from the response when all requested deletions succeed.",
              "items": {
                "type": "object",
                "properties": {
                  "modelId": {
                    "type": "string"
                  },
                  "message": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModels": {
      "description": "Minimum role: Standard User.\n\nReturns models accessible to the calling user within the specified workspace. The response shape is identical to `GET /2/0/models` but filtered to models within the given workspace.\n\nModels with `activeState` of `ARCHIVED` are typically inactive and not actionable. Filter them out of results (keep only `activeState` != `ARCHIVED`) unless you specifically need archived models.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "models": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string",
                    "pattern": "^[0-9a-fA-F]{32}$"
                  },
                  "name": {
                    "type": "string"
                  },
                  "activeState": {
                    "type": "string",
                    "enum": [
                      "MAINTENANCE",
                      "UNLOCKED",
                      "PRODUCTION",
                      "ARCHIVED",
                      "PRODUCTION_MAINTENANCE",
                      "LOCKED"
                    ]
                  },
                  "currentWorkspaceId": {
                    "type": "string"
                  },
                  "currentWorkspaceName": {
                    "type": "string"
                  },
                  "modelUrl": {
                    "type": "string"
                  },
                  "categoryValues": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "id": {
                          "type": "string"
                        },
                        "attribute": {
                          "type": "string"
                        },
                        "categoryId": {
                          "type": "string"
                        },
                        "categoryName": {
                          "type": "string"
                        },
                        "customerId": {
                          "type": "string"
                        }
                      }
                    }
                  },
                  "isoCreationDate": {
                    "type": "string"
                  },
                  "lastModified": {
                    "type": "string"
                  },
                  "lastModifiedByUserGuid": {
                    "type": "string"
                  },
                  "lastSavedSerialNumber": {
                    "type": "string"
                  },
                  "memoryUsage": {
                    "type": "number"
                  },
                  "modelTransactionRunning": {
                    "type": "boolean"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidActionsByActionidTasks": {
      "description": "Minimum role: Standard User.\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#\n\n**Note:** Returns `200` with an empty task list rather than `404` when the parent object does not exist, or when the caller holds a NO ACCESS role on the model.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "actionId",
          "in": "path",
          "description": "Action identifier. From `GET /models/{modelId}/actions`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort order for the task list. Format: [prefix]field where prefix is - (descending), + (ascending), or omitted (ascending). Documented sortable fields: creationDate, taskId, taskState, progress. Example: -creationDate (newest first), +taskId, taskState. The API does not validate the field name \u2014 unknown fields are silently accepted and appear to fall back to default ordering.",
          "required": false,
          "schema": {
            "type": "string"
          },
          "example": "-creationDate"
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "tasks": {
              "type": "array",
              "items": {
                "description": "Initial task reference returned by POST .../tasks. Contains the taskId and a URL for polling status.",
                "properties": {
                  "creationTime": {
                    "type": "integer",
                    "description": "Task creation time in milliseconds since epoch."
                  },
                  "taskId": {
                    "type": "string",
                    "description": "Unique identifier for the task. Use this to poll GET .../tasks/{taskId}."
                  },
                  "taskState": {
                    "enum": [
                      "NOT_STARTED",
                      "IN_PROGRESS",
                      "COMPLETE",
                      "CANCELLING",
                      "CANCELLED"
                    ],
                    "description": "Lifecycle state of the task."
                  },
                  "taskUrl": {
                    "type": "string",
                    "description": "URL for polling task status via GET."
                  }
                }
              }
            }
          }
        }
      }
    },
    "postWorkspacesByWorkspaceidModelsByModelidActionsByActionidTasks": {
      "description": "Minimum role: Standard User.\n\nYou can POST a deletion task to start the delete. This returns a new `taskID`. Once you have started the delete, you can use GET to receive a list of the deletion tasks as a JSON array. You can use this to monitor the status of the delete.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "actionId",
          "in": "path",
          "description": "Action identifier. From `GET /models/{modelId}/actions`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "task": {
              "description": "Initial task reference returned by POST .../tasks. Contains the taskId and a URL for polling status.",
              "properties": {
                "creationTime": {
                  "type": "integer",
                  "description": "Task creation time in milliseconds since epoch."
                },
                "taskId": {
                  "type": "string",
                  "description": "Unique identifier for the task. Use this to poll GET .../tasks/{taskId}."
                },
                "taskState": {
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ],
                  "description": "Lifecycle state of the task."
                },
                "taskUrl": {
                  "type": "string",
                  "description": "URL for polling task status via GET."
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidActionsByActionidTasksByTaskid": {
      "description": "Minimum role: Standard User.\n\nReturns the current state of a single action task. Poll this endpoint after starting an action via POST to monitor progress.\n\nPossible `taskState` values: `NOT_STARTED`, `IN_PROGRESS`, `COMPLETE`, `CANCELLING`, `CANCELLED`.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "actionId",
          "in": "path",
          "description": "Action identifier. From `GET /models/{modelId}/actions`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "taskId",
          "in": "path",
          "description": "Task identifier, scoped to the parent action/import/export/process that launched it: a taskId is valid only under the same parent, so an action's taskId will not resolve under a process, import, or export. Returned when a run is launched by `POST /workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks` (or the equivalent actions, exports, or processes tasks endpoint), and listed by the matching `GET .../tasks` \u2014 `tasks[].taskId`, except the import tasks response, which nests it under `task.taskId`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "task": {
              "description": "A task tracking the execution of a process/import/export/action",
              "properties": {
                "creationTime": {
                  "type": "integer",
                  "description": "Task creation time in milliseconds since epoch"
                },
                "currentStep": {
                  "type": "string",
                  "description": "Human-readable description of the current execution step"
                },
                "progress": {
                  "type": "number",
                  "description": "Execution progress as a fraction (0.0\u20131.0). The value is typically 1.0 on completion."
                },
                "result": {
                  "description": "The result of a completed task",
                  "properties": {
                    "details": {
                      "items": {
                        "properties": {
                          "localMessageText": {
                            "type": "string"
                          },
                          "occurrences": {
                            "type": "integer"
                          },
                          "type": {
                            "type": "string"
                          },
                          "values": {
                            "items": {
                              "type": "string"
                            },
                            "type": "array"
                          }
                        },
                        "type": "object"
                      },
                      "type": "array"
                    },
                    "duration": {
                      "type": "string"
                    },
                    "failureDumpAvailable": {
                      "type": "boolean",
                      "description": "Whether a failure dump file was generated for this task. When true, the dump can be downloaded via the failure dump endpoint for this object type."
                    },
                    "nestedResults": {
                      "items": {
                        "x-ptc-truncated": "recursive schema, truncated at #/components/schemas/TaskResult"
                      },
                      "type": "array"
                    },
                    "objectId": {
                      "type": "string",
                      "description": "Numeric ID of the Anaplan object (import, export, process, or action) that this task executed against."
                    },
                    "objectName": {
                      "type": "string",
                      "description": "Display name of the Anaplan object that this task executed against."
                    },
                    "startTime": {
                      "type": "string"
                    },
                    "successful": {
                      "type": "boolean"
                    }
                  }
                },
                "taskId": {
                  "type": "string"
                },
                "taskState": {
                  "description": "Lifecycle state of the task",
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ]
                }
              }
            }
          }
        }
      },
      "response_truncated": true
    },
    "getWorkspacesByWorkspaceidModelsByModelidDashboards": {
      "description": "Minimum role: Standard User.\n\nReturns all dashboards defined in the specified model.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "dashboards": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  },
                  "code": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidExportsByExportidTasks": {
      "description": "Minimum role: Standard User.\n\n**Note:** Returns `200` with an empty task list rather than `404` when the parent object does not exist, or when the caller holds a NO ACCESS role on the model.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "exportId",
          "in": "path",
          "description": "Export definition identifier (12-digit numeric, prefix 116). From `GET /models/{modelId}/exports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort order for the task list. Format: [prefix]field where prefix is - (descending), + (ascending), or omitted (ascending). Documented sortable fields: creationDate, taskId, taskState, progress. Example: -creationDate (newest first), +taskId, taskState. The API does not validate the field name \u2014 unknown fields are silently accepted and appear to fall back to default ordering.",
          "required": false,
          "schema": {
            "type": "string"
          },
          "example": "-creationDate"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "tasks": {
              "type": "array",
              "items": {
                "description": "A task tracking the execution of a process/import/export/action",
                "properties": {
                  "creationTime": {
                    "type": "integer",
                    "description": "Task creation time in milliseconds since epoch"
                  },
                  "currentStep": {
                    "type": "string",
                    "description": "Human-readable description of the current execution step"
                  },
                  "progress": {
                    "type": "number",
                    "description": "Execution progress as a fraction (0.0\u20131.0). The value is typically 1.0 on completion."
                  },
                  "result": {
                    "description": "The result of a completed task",
                    "properties": {
                      "details": {
                        "items": {
                          "properties": {
                            "localMessageText": {
                              "type": "string"
                            },
                            "occurrences": {
                              "type": "integer"
                            },
                            "type": {
                              "type": "string"
                            },
                            "values": {
                              "items": {
                                "type": "string"
                              },
                              "type": "array"
                            }
                          },
                          "type": "object"
                        },
                        "type": "array"
                      },
                      "duration": {
                        "type": "string"
                      },
                      "failureDumpAvailable": {
                        "type": "boolean",
                        "description": "Whether a failure dump file was generated for this task. When true, the dump can be downloaded via the failure dump endpoint for this object type."
                      },
                      "nestedResults": {
                        "items": {
                          "x-ptc-truncated": "recursive schema, truncated at #/components/schemas/TaskResult"
                        },
                        "type": "array"
                      },
                      "objectId": {
                        "type": "string",
                        "description": "Numeric ID of the Anaplan object (import, export, process, or action) that this task executed against."
                      },
                      "objectName": {
                        "type": "string",
                        "description": "Display name of the Anaplan object that this task executed against."
                      },
                      "startTime": {
                        "type": "string"
                      },
                      "successful": {
                        "type": "boolean"
                      }
                    }
                  },
                  "taskId": {
                    "type": "string"
                  },
                  "taskState": {
                    "description": "Lifecycle state of the task",
                    "enum": [
                      "NOT_STARTED",
                      "IN_PROGRESS",
                      "COMPLETE",
                      "CANCELLING",
                      "CANCELLED"
                    ]
                  }
                }
              }
            }
          }
        }
      },
      "response_truncated": true
    },
    "postWorkspacesByWorkspaceidModelsByModelidExportsByExportidTasks": {
      "description": "Minimum role: Standard User.\n\nYou can POST an export task to start the export. This returns a new `taskID`. Once you have started the export, you can use GET to receive a list of the export tasks as a JSON array. You can use this to monitor the status of the export tasks.\n\n### Run the export\n\nThis creates a task with the specified exportID and returns the newly-created taskID.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "exportId",
          "in": "path",
          "description": "Export definition identifier (12-digit numeric, prefix 116). From `GET /models/{modelId}/exports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": {
        "media_type": "application/json",
        "required": false,
        "schema": {
          "type": "object",
          "required": [
            "localeName"
          ],
          "properties": {
            "localeName": {
              "type": "string",
              "description": "Locale for the export.",
              "example": "en_US"
            }
          }
        }
      },
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "task": {
              "description": "A task tracking the execution of a process/import/export/action",
              "properties": {
                "creationTime": {
                  "type": "integer",
                  "description": "Task creation time in milliseconds since epoch"
                },
                "currentStep": {
                  "type": "string",
                  "description": "Human-readable description of the current execution step"
                },
                "progress": {
                  "type": "number",
                  "description": "Execution progress as a fraction (0.0\u20131.0). The value is typically 1.0 on completion."
                },
                "result": {
                  "description": "The result of a completed task",
                  "properties": {
                    "details": {
                      "items": {
                        "properties": {
                          "localMessageText": {
                            "type": "string"
                          },
                          "occurrences": {
                            "type": "integer"
                          },
                          "type": {
                            "type": "string"
                          },
                          "values": {
                            "items": {
                              "type": "string"
                            },
                            "type": "array"
                          }
                        },
                        "type": "object"
                      },
                      "type": "array"
                    },
                    "duration": {
                      "type": "string"
                    },
                    "failureDumpAvailable": {
                      "type": "boolean",
                      "description": "Whether a failure dump file was generated for this task. When true, the dump can be downloaded via the failure dump endpoint for this object type."
                    },
                    "nestedResults": {
                      "items": {
                        "x-ptc-truncated": "recursive schema, truncated at #/components/schemas/TaskResult"
                      },
                      "type": "array"
                    },
                    "objectId": {
                      "type": "string",
                      "description": "Numeric ID of the Anaplan object (import, export, process, or action) that this task executed against."
                    },
                    "objectName": {
                      "type": "string",
                      "description": "Display name of the Anaplan object that this task executed against."
                    },
                    "startTime": {
                      "type": "string"
                    },
                    "successful": {
                      "type": "boolean"
                    }
                  }
                },
                "taskId": {
                  "type": "string"
                },
                "taskState": {
                  "description": "Lifecycle state of the task",
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ]
                }
              }
            }
          }
        }
      },
      "response_truncated": true
    },
    "getWorkspacesByWorkspaceidModelsByModelidExportsByExportidTasksByTaskid": {
      "description": "Minimum role: Standard User.\n\nYou can get the status of a specific export task. In the response:\n\n*   `currentStep` is the task associated with the specified `taskID`.\n*   `taskState` is the status of that export task.\n*   `progress` is a double that is typically 1.0.\n*   `result` is a JSON object that indicates the object ID, and whether the task succeeded.\n*   `taskId` is the ID that you specified in the request.\n    \n\nHere is a list of each possible `taskState`:\n\n| **Status** | **Description** |\n| --- | --- |\n| NOT_STARTED | The task is scheduled to run, but has not started yet. |\n| IN_PROGRESS | The task is currently running. |\n| COMPLETE | The task has finished running whether successful or not. |\n| CANCELLING | The task is currently being canceled. Cancellation is not yet complete. |\n| CANCELLED | The task has been canceled and the changes have been rolled back. |\n\nIf failures occur during the export task `details` contains a JSON array that contains debug information\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "exportId",
          "in": "path",
          "description": "Export definition identifier (12-digit numeric, prefix 116). From `GET /models/{modelId}/exports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "taskId",
          "in": "path",
          "description": "Task identifier, scoped to the parent action/import/export/process that launched it: a taskId is valid only under the same parent, so an action's taskId will not resolve under a process, import, or export. Returned when a run is launched by `POST /workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks` (or the equivalent actions, exports, or processes tasks endpoint), and listed by the matching `GET .../tasks` \u2014 `tasks[].taskId`, except the import tasks response, which nests it under `task.taskId`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "task": {
              "description": "A task tracking the execution of a process/import/export/action",
              "properties": {
                "creationTime": {
                  "type": "integer",
                  "description": "Task creation time in milliseconds since epoch"
                },
                "currentStep": {
                  "type": "string",
                  "description": "Human-readable description of the current execution step"
                },
                "progress": {
                  "type": "number",
                  "description": "Execution progress as a fraction (0.0\u20131.0). The value is typically 1.0 on completion."
                },
                "result": {
                  "description": "The result of a completed task",
                  "properties": {
                    "details": {
                      "items": {
                        "properties": {
                          "localMessageText": {
                            "type": "string"
                          },
                          "occurrences": {
                            "type": "integer"
                          },
                          "type": {
                            "type": "string"
                          },
                          "values": {
                            "items": {
                              "type": "string"
                            },
                            "type": "array"
                          }
                        },
                        "type": "object"
                      },
                      "type": "array"
                    },
                    "duration": {
                      "type": "string"
                    },
                    "failureDumpAvailable": {
                      "type": "boolean",
                      "description": "Whether a failure dump file was generated for this task. When true, the dump can be downloaded via the failure dump endpoint for this object type."
                    },
                    "nestedResults": {
                      "items": {
                        "x-ptc-truncated": "recursive schema, truncated at #/components/schemas/TaskResult"
                      },
                      "type": "array"
                    },
                    "objectId": {
                      "type": "string",
                      "description": "Numeric ID of the Anaplan object (import, export, process, or action) that this task executed against."
                    },
                    "objectName": {
                      "type": "string",
                      "description": "Display name of the Anaplan object that this task executed against."
                    },
                    "startTime": {
                      "type": "string"
                    },
                    "successful": {
                      "type": "boolean"
                    }
                  }
                },
                "taskId": {
                  "type": "string"
                },
                "taskState": {
                  "description": "Lifecycle state of the task",
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ]
                }
              }
            }
          }
        }
      },
      "response_truncated": true
    },
    "getWorkspacesByWorkspaceidModelsByModelidFilesByFileid": {
      "description": "Minimum role: Standard User.\n\nReturns the raw content of the file as `application/octet-stream` \u2014 the file body itself, not a JSON metadata envelope. Response bytes are identical to concatenating every chunk from `GET .../files/{fileId}/chunks/{chunkId}`, so this is the single-call alternative to the chunked download path.\n\n**HIGH VOLUME** \u2014 this returns the complete file in one response (observed ~462 KB, and files can be far larger). For anything but tiny files use the chunked download path instead: `GET /workspaces/{workspaceId}/models/{modelId}/files/{fileId}/chunks` to enumerate chunks, then `GET /workspaces/{workspaceId}/models/{modelId}/files/{fileId}/chunks/{chunkId}` to fetch each \u2014 never hold the whole body in one response.\n\nThis endpoint returns no metadata. Per-file metadata (`name`, `chunkCount`, `delimiter`, `encoding`, `format`, `headerRow`, `separator`) comes from the file list endpoint `GET /models/{modelId}/files`, which lists every file in the model.\n\nA file only has downloadable content once content has been uploaded to it or written by an export action. Files listed with `chunkCount` of 0 hold no content and return `404`.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "fileId",
          "in": "path",
          "description": "File identifier (12-digit numeric, prefix 113). From `GET /models/{modelId}/files`, or the `importDataSourceId` of `GET /workspaces/{workspaceId}/models/{modelId}/imports/` \u2014 the file an import reads, which is the file you upload before running that import.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": null,
      "response_note": "no documented 2xx JSON response schema \u2014 probe a read operation to learn it"
    },
    "postWorkspacesByWorkspaceidModelsByModelidFilesByFileid": {
      "description": "Minimum role: Standard User.\n\nTo upload a file, we recommend setting the `chunkcount` metadata of an upload so that the server knows how many chunks to process during an upload. Set the `chunkcount` to -1 if you do not know how many chunks there will be and will be marking the upload complete when all chunks have been uploaded. If you want to upload the file in a single chunk, see Upload file as a single chunk.\n\nUse chunking:\n\n*   If you want to be able to resume the transfer after the connection is lost during an upload.\n*   If you are extracting data from a database and want to push it to the server without holding all the results in memory.\n    \n\nTo avoid consuming large amounts of memory on the server, a chunk is expected to be between 1 and 50 MB. The chunk count might change if the file to upload has become larger or smaller than the server's version of that file:\n\n*   `id` is the ID of the file for which the chunk count is to be updated.\n*   `name` is the filename of the file.\n*   `chunkCount` is the number of chunks in the file.\n*   `delimiter` is double-quotes for columns and fields. For example, \"Versions\",\"Opportunities\",\" \"Version 1\",\"Opportunity 0001\".\n*   `encoding` determines the way in which bytes in a file map to Unicode character code points. See [Common Character Encodings](https://en.wikipedia.org/wiki/Character_encoding#Common_character_encodings) on Wikipedia.\n*   `firstDataRow` is the first row after the row containing column names.\n*   `format` is text, as opposed to a binary stream.\n*   `headerRow` is the row that contains column names.\n*   `separator` is the token that separates fields. This is typically a comma, for example, \"Versions\",\"Opportunities\",\" \"Version 1\",\"Opportunity 0001\".\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "fileId",
          "in": "path",
          "description": "File identifier (12-digit numeric, prefix 113). From `GET /models/{modelId}/files`, or the `importDataSourceId` of `GET /workspaces/{workspaceId}/models/{modelId}/imports/` \u2014 the file an import reads, which is the file you upload before running that import.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": {
        "media_type": "application/json",
        "required": false,
        "schema": {
          "type": "object",
          "properties": {
            "chunkCount": {
              "type": "integer"
            }
          }
        }
      },
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "file": {
              "description": "A file associated with an import data source, import failure dump, or export",
              "properties": {
                "chunkCount": {
                  "type": "integer"
                },
                "code": {
                  "type": "string"
                },
                "country": {
                  "type": "string"
                },
                "delimiter": {
                  "type": "string"
                },
                "encoding": {
                  "type": "string",
                  "description": "Character encoding of the file. Observed values: ISO-8859-1, UTF-16LE, UTF-8."
                },
                "firstDataRow": {
                  "type": "integer",
                  "description": "Row number of the first data row. 0 means no data rows are configured. Other values are 1-based (row 1 = first row of the file)."
                },
                "format": {
                  "type": "string"
                },
                "headerRow": {
                  "type": "integer",
                  "description": "1-based row number of the column-header row in a delimited file."
                },
                "id": {
                  "type": "string",
                  "description": "12-digit numeric identifier. Import source files use the prefix 113. Export output files use 116, matching their parent export's ID."
                },
                "language": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "origin": {
                  "type": "string",
                  "description": "Source type of the file: 'NATIVE_ANAPLAN' for Anaplan-owned files, 'FILE_BASED' for externally uploaded import sources.",
                  "example": "NATIVE_ANAPLAN"
                },
                "separator": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "putWorkspacesByWorkspaceidModelsByModelidFilesByFileid": {
      "description": "Minimum role: Standard User.\n\nYou can upload a single file to the server.\n\n**Note:** You cannot compress a file that you are uploading as a single file. If the file is smaller than 1 MB, we recommend you PUT it onto the server by streaming it as an octet stream.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/octet-stream`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "fileId",
          "in": "path",
          "description": "File identifier (12-digit numeric, prefix 113). From `GET /models/{modelId}/files`, or the `importDataSourceId` of `GET /workspaces/{workspaceId}/models/{modelId}/imports/` \u2014 the file an import reads, which is the file you upload before running that import.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": {
        "media_type": "application/octet-stream",
        "required": true,
        "schema": {
          "type": "string",
          "format": "binary"
        }
      },
      "response": null,
      "response_note": "no documented 2xx JSON response schema \u2014 probe a read operation to learn it"
    },
    "deleteWorkspacesByWorkspaceidModelsByModelidFilesByFileid": {
      "description": "Minimum role: Standard User.\n\nYou can delete files that you have uploaded using the API.\n\n**Note:** This only removes private content. Default content and the import data source model object remain.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "fileId",
          "in": "path",
          "description": "File identifier (12-digit numeric, prefix 113). From `GET /models/{modelId}/files`, or the `importDataSourceId` of `GET /workspaces/{workspaceId}/models/{modelId}/imports/` \u2014 the file an import reads, which is the file you upload before running that import.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": null,
      "response_note": "no documented 2xx JSON response schema \u2014 probe a read operation to learn it"
    },
    "getWorkspacesByWorkspaceidModelsByModelidFilesByFileidChunksByChunkid": {
      "description": "Minimum role: Standard User.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "fileId",
          "in": "path",
          "description": "File identifier (12-digit numeric, prefix 113). From `GET /models/{modelId}/files`, or the `importDataSourceId` of `GET /workspaces/{workspaceId}/models/{modelId}/imports/` \u2014 the file an import reads, which is the file you upload before running that import.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "chunkId",
          "in": "path",
          "description": "The chunk identifier. From the `id` values returned by the sibling `GET .../chunks` list endpoint for the file or dump. Chunk ids are zero-based. Chunk `0` is always the first chunk of a file that has any content.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": null,
      "response_note": "no documented 2xx JSON response schema \u2014 probe a read operation to learn it"
    },
    "putWorkspacesByWorkspaceidModelsByModelidFilesByFileidChunksByChunkid": {
      "description": "Minimum role: Standard User.\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/octet-stream`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "fileId",
          "in": "path",
          "description": "File identifier (12-digit numeric, prefix 113). From `GET /models/{modelId}/files`, or the `importDataSourceId` of `GET /workspaces/{workspaceId}/models/{modelId}/imports/` \u2014 the file an import reads, which is the file you upload before running that import.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "chunkId",
          "in": "path",
          "description": "The chunk identifier. From the `id` values returned by the sibling `GET .../chunks` list endpoint for the file or dump. Chunk ids are zero-based. Chunk `0` is always the first chunk of a file that has any content.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": {
        "media_type": "application/octet-stream",
        "required": true,
        "schema": {
          "type": "string",
          "format": "binary"
        }
      },
      "response": null,
      "response_note": "no documented 2xx JSON response schema \u2014 probe a read operation to learn it"
    },
    "postWorkspacesByWorkspaceidModelsByModelidFilesByFileidComplete": {
      "description": "Minimum role: Standard User.\n\nAfter carrying out an upload with `chunkCount` set to -1, mark it complete.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "fileId",
          "in": "path",
          "description": "File identifier (12-digit numeric, prefix 113). From `GET /models/{modelId}/files`, or the `importDataSourceId` of `GET /workspaces/{workspaceId}/models/{modelId}/imports/` \u2014 the file an import reads, which is the file you upload before running that import.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "file": {
              "description": "A file associated with an import data source, import failure dump, or export",
              "properties": {
                "chunkCount": {
                  "type": "integer"
                },
                "code": {
                  "type": "string"
                },
                "country": {
                  "type": "string"
                },
                "delimiter": {
                  "type": "string"
                },
                "encoding": {
                  "type": "string",
                  "description": "Character encoding of the file. Observed values: ISO-8859-1, UTF-16LE, UTF-8."
                },
                "firstDataRow": {
                  "type": "integer",
                  "description": "Row number of the first data row. 0 means no data rows are configured. Other values are 1-based (row 1 = first row of the file)."
                },
                "format": {
                  "type": "string"
                },
                "headerRow": {
                  "type": "integer",
                  "description": "1-based row number of the column-header row in a delimited file."
                },
                "id": {
                  "type": "string",
                  "description": "12-digit numeric identifier. Import source files use the prefix 113. Export output files use 116, matching their parent export's ID."
                },
                "language": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "origin": {
                  "type": "string",
                  "description": "Source type of the file: 'NATIVE_ANAPLAN' for Anaplan-owned files, 'FILE_BASED' for externally uploaded import sources.",
                  "example": "NATIVE_ANAPLAN"
                },
                "separator": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasks": {
      "description": "Minimum role: Standard User.\n\n*   Request\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#\n\n**Note:** Returns `200` with an empty task list rather than `404` when the parent object does not exist, or when the caller holds a NO ACCESS role on the model.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "importId",
          "in": "path",
          "description": "Import definition identifier (12-digit numeric, prefix 112). From `GET /models/{modelId}/imports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort order for the task list. Format: [prefix]field where prefix is - (descending), + (ascending), or omitted (ascending). Documented sortable fields: creationDate, taskId, taskState, progress. Example: -creationDate (newest first), +taskId, taskState. The API does not validate the field name \u2014 unknown fields are silently accepted and appear to fall back to default ordering.",
          "required": false,
          "schema": {
            "type": "string"
          },
          "example": "-creationDate"
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "task": {
              "description": "A task tracking the execution of a process/import/export/action",
              "properties": {
                "creationTime": {
                  "type": "integer",
                  "description": "Task creation time in milliseconds since epoch"
                },
                "currentStep": {
                  "type": "string",
                  "description": "Human-readable description of the current execution step"
                },
                "progress": {
                  "type": "number",
                  "description": "Execution progress as a fraction (0.0\u20131.0). The value is typically 1.0 on completion."
                },
                "result": {
                  "description": "The result of a completed task",
                  "properties": {
                    "details": {
                      "items": {
                        "properties": {
                          "localMessageText": {
                            "type": "string"
                          },
                          "occurrences": {
                            "type": "integer"
                          },
                          "type": {
                            "type": "string"
                          },
                          "values": {
                            "items": {
                              "type": "string"
                            },
                            "type": "array"
                          }
                        },
                        "type": "object"
                      },
                      "type": "array"
                    },
                    "duration": {
                      "type": "string"
                    },
                    "failureDumpAvailable": {
                      "type": "boolean",
                      "description": "Whether a failure dump file was generated for this task. When true, the dump can be downloaded via the failure dump endpoint for this object type."
                    },
                    "nestedResults": {
                      "items": {
                        "x-ptc-truncated": "recursive schema, truncated at #/components/schemas/TaskResult"
                      },
                      "type": "array"
                    },
                    "objectId": {
                      "type": "string",
                      "description": "Numeric ID of the Anaplan object (import, export, process, or action) that this task executed against."
                    },
                    "objectName": {
                      "type": "string",
                      "description": "Display name of the Anaplan object that this task executed against."
                    },
                    "startTime": {
                      "type": "string"
                    },
                    "successful": {
                      "type": "boolean"
                    }
                  }
                },
                "taskId": {
                  "type": "string"
                },
                "taskState": {
                  "description": "Lifecycle state of the task",
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ]
                }
              }
            }
          }
        }
      },
      "response_truncated": true
    },
    "postWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasks": {
      "description": "Minimum role: Standard User.\n\nYou start the import using the POST command. This also receives the task ID value as a JSON object. Once you have started the import, you can use GET to receive a list of the import tasks as a JSON array. You can use this to monitor the status of the import tasks.\n\n**Note:** If GET does not return any tasks, the import is not running or might have completed. Tasks sort in ascending order of creation time, with the newest task at the top of the list. To sort in descending order, append `?sort =- creationTime` to the URL.\n\n### Run the import\n\n*   Request\n\n### Request Body\n\n| **Body** | **Description** |\n| --- | --- |\n| `{\"localeName\": \"en_US\"}` | <br><br>*   Required<br>*   Description: Specifies the language to use for task status and error messages. `en_US` is the only supported language type.<br><br> |\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "importId",
          "in": "path",
          "description": "Import definition identifier (12-digit numeric, prefix 112). From `GET /models/{modelId}/imports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "description": "Initial task reference returned by POST .../tasks. Contains the taskId and a URL for polling status.",
          "properties": {
            "creationTime": {
              "type": "integer",
              "description": "Task creation time in milliseconds since epoch."
            },
            "taskId": {
              "type": "string",
              "description": "Unique identifier for the task. Use this to poll GET .../tasks/{taskId}."
            },
            "taskState": {
              "enum": [
                "NOT_STARTED",
                "IN_PROGRESS",
                "COMPLETE",
                "CANCELLING",
                "CANCELLED"
              ],
              "description": "Lifecycle state of the task."
            },
            "taskUrl": {
              "type": "string",
              "description": "URL for polling task status via GET."
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasksByTaskid": {
      "description": "Minimum role: Standard User.\n\nIn the response:\n\n*   `currentStep` is the task associated with the specified `taskID`.\n*   `taskState` is the status that import task.\n*   `progress` is a double that is typically 1.0.\n*   `result` is a JSON object that indicates whether a failure dump file was created, the object ID, and whether the task succeeded.\n*   `taskID` is the ID that you specified in the request.\n    \n\nHere is a list of each possible `taskState`:\n\n| **Status** | **Description** |\n| --- | --- |\n| `NOT_STARTED` | The task is scheduled to run, but has not started yet. |\n| `IN_PROGRESS` | The task is currently running. |\n| `COMPLETE` | The task has finished running whether successful or not. |\n| `CANCELLING` | The task is currently being canceled. Cancellation is not yet complete. |\n| `CANCELLED` | The task has been canceled and the changes have been rolled back. |\n\nIf failures occur during the import task:\n\n*   `details` contains a JSON array that contains debug information\n*   If a dump file is available, `failureDumpAvailable` is true and `objectId` contains the ID of the dump file. The dump file contains details of the failures that occurred during the import. You can use the `objectId` to get the dump file.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "importId",
          "in": "path",
          "description": "Import definition identifier (12-digit numeric, prefix 112). From `GET /models/{modelId}/imports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "taskId",
          "in": "path",
          "description": "Task identifier, scoped to the parent action/import/export/process that launched it: a taskId is valid only under the same parent, so an action's taskId will not resolve under a process, import, or export. Returned when a run is launched by `POST /workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks` (or the equivalent actions, exports, or processes tasks endpoint), and listed by the matching `GET .../tasks` \u2014 `tasks[].taskId`, except the import tasks response, which nests it under `task.taskId`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "task": {
              "description": "A task tracking the execution of a process/import/export/action",
              "properties": {
                "creationTime": {
                  "type": "integer",
                  "description": "Task creation time in milliseconds since epoch"
                },
                "currentStep": {
                  "type": "string",
                  "description": "Human-readable description of the current execution step"
                },
                "progress": {
                  "type": "number",
                  "description": "Execution progress as a fraction (0.0\u20131.0). The value is typically 1.0 on completion."
                },
                "result": {
                  "description": "The result of a completed task",
                  "properties": {
                    "details": {
                      "items": {
                        "properties": {
                          "localMessageText": {
                            "type": "string"
                          },
                          "occurrences": {
                            "type": "integer"
                          },
                          "type": {
                            "type": "string"
                          },
                          "values": {
                            "items": {
                              "type": "string"
                            },
                            "type": "array"
                          }
                        },
                        "type": "object"
                      },
                      "type": "array"
                    },
                    "duration": {
                      "type": "string"
                    },
                    "failureDumpAvailable": {
                      "type": "boolean",
                      "description": "Whether a failure dump file was generated for this task. When true, the dump can be downloaded via the failure dump endpoint for this object type."
                    },
                    "nestedResults": {
                      "items": {
                        "x-ptc-truncated": "recursive schema, truncated at #/components/schemas/TaskResult"
                      },
                      "type": "array"
                    },
                    "objectId": {
                      "type": "string",
                      "description": "Numeric ID of the Anaplan object (import, export, process, or action) that this task executed against."
                    },
                    "objectName": {
                      "type": "string",
                      "description": "Display name of the Anaplan object that this task executed against."
                    },
                    "startTime": {
                      "type": "string"
                    },
                    "successful": {
                      "type": "boolean"
                    }
                  }
                },
                "taskId": {
                  "type": "string"
                },
                "taskState": {
                  "description": "Lifecycle state of the task",
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ]
                }
              }
            }
          }
        }
      },
      "response_truncated": true
    },
    "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasksByTaskidDump": {
      "description": "Minimum role: Standard User.\n\nIf data could not be imported for an import task, a 'dump' file is created depending on the failure. This contains details of the failures that occurred. Use this information to fix the problems and then try the import again.\n\n**Note:** To prevent timeouts on large files, download the dump file in chunks. You can download dump files without chunking, but chunking is strongly recommended.\n\n1.  Query the number of chunks in a dump file.  \n    \n2.  Download the chunks sequentially.  \n    \n3.  Concatenate the chunks on your host.\n    \n\nDump files are:\n\n*   Removed 48 hours after the last access of the file or the next time an import task successfully executes with no errors.\n*   Maintained on a user-by-user basis and overwritten when the user performs the same import.\n    \n\nWhen an import task has errors, the API maintains a dump file for the task ID with the error.  Any earlier task IDs are erased, though `failureDumpAvailable` can be True for those task IDs.  If a dump file is removed or replaced, then attempting to download the dump file via the API results in a 404 (Not found) HTTP status.\n\nIf you use `/tasks?sort=-creationDate` in the URL, tasks created by other users can appear first, and their dump files are not available to the accessing user.  A task that the accessing user ran, and whose dump file is still available, can appear further down the same list.\n\n**Note:** The `taskID` is the last task of the user who carried out the specified import.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "importId",
          "in": "path",
          "description": "Import definition identifier (12-digit numeric, prefix 112). From `GET /models/{modelId}/imports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "taskId",
          "in": "path",
          "description": "Task identifier, scoped to the parent action/import/export/process that launched it: a taskId is valid only under the same parent, so an action's taskId will not resolve under a process, import, or export. Returned when a run is launched by `POST /workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks` (or the equivalent actions, exports, or processes tasks endpoint), and listed by the matching `GET .../tasks` \u2014 `tasks[].taskId`, except the import tasks response, which nests it under `task.taskId`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": null,
      "response_note": "no documented 2xx JSON response schema \u2014 probe a read operation to learn it"
    },
    "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasksByTaskidDumpChunks": {
      "description": "Minimum role: Standard User.\n\nBegin by querying the number of chunks using the `/chunks` endpoint.\n\nA dump file can be very large. To help manage the size of a dump file and prevent timeouts, download the dump file in chunks. Chunks are 10mb or less and numbered sequentially from 0.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "importId",
          "in": "path",
          "description": "Import definition identifier (12-digit numeric, prefix 112). From `GET /models/{modelId}/imports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "taskId",
          "in": "path",
          "description": "Task identifier, scoped to the parent action/import/export/process that launched it: a taskId is valid only under the same parent, so an action's taskId will not resolve under a process, import, or export. Returned when a run is launched by `POST /workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks` (or the equivalent actions, exports, or processes tasks endpoint), and listed by the matching `GET .../tasks` \u2014 `tasks[].taskId`, except the import tasks response, which nests it under `task.taskId`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "chunks": {
              "type": "array",
              "items": {
                "description": "A unit of partial file content",
                "properties": {
                  "code": {
                    "type": "string"
                  },
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidImportsByImportidTasksByTaskidDumpChunksByChunkid": {
      "description": "Minimum role: Standard User.\n\nOnce you have the chunk count, download them sequentially and concatenate. Download each chunk by its `id` (from the `.../dump/chunks` list).\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "importId",
          "in": "path",
          "description": "Import definition identifier (12-digit numeric, prefix 112). From `GET /models/{modelId}/imports`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "taskId",
          "in": "path",
          "description": "Task identifier, scoped to the parent action/import/export/process that launched it: a taskId is valid only under the same parent, so an action's taskId will not resolve under a process, import, or export. Returned when a run is launched by `POST /workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks` (or the equivalent actions, exports, or processes tasks endpoint), and listed by the matching `GET .../tasks` \u2014 `tasks[].taskId`, except the import tasks response, which nests it under `task.taskId`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "chunkId",
          "in": "path",
          "description": "The chunk identifier. From the `id` values returned by the sibling `GET .../chunks` list endpoint for the file or dump. Chunk ids are zero-based. Chunk `0` is always the first chunk of a file that has any content.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": null,
      "response_note": "no documented 2xx JSON response schema \u2014 probe a read operation to learn it"
    },
    "getWorkspacesByWorkspaceidModelsByModelidListsByListidItems": {
      "description": "Minimum role: Standard User.\n\nReturns the list data of the items in a list.\n\nYou can filter on optional fields by setting the `includeAll` query parameter.\n\n**Notes:**\n\n*   Use of this call may be rate limited. If you call too frequently, you may receive a `429` response.\n    \n\nThe following list data always displays in the query response:\n\n| **Default List Data** | **Description** |\n| --- | --- |\n| `id` | The Anaplan ID of the item in the list. |\n| `name` | The name of the item in the list. |\n| `code` | The code value for an item in the list (if specified) |\n| `parent` | The name of the parent item in the list. |\n| `parentId` | The Anaplan ID of the parent item in the list. |\n| `listId` | The Anaplan ID of the list to which the parent item belongs. This ID displays if the list has a **Parent Hierarchy** set under the **Configure** tab in Anaplan. |\n| `listName` | The name of list to which the parent item belongs. The name displays if the list has a **Parent Hierarchy** set under the **Configure** tab in Anaplan. |\n\nSet the `includeAll` query parameter to true to display the following optional list data fields in the response.\n\n| **Optional List Data** | **Description** |\n| --- | --- |\n| `write` | If [Selective Access](https://help.anaplan.com/anapedia/Content/Modeling/Users/Selective_Access.html) is enabled for the list, you can add individual users to provide `write` access. Users with `write` access can view and edit data associated with the list items. |\n| `read` | If [Selective Access](https://help.anaplan.com/anapedia/Content/Modeling/Users/Selective_Access.html) is enabled for the list, you can add individual users to provide `read` access. Users with `read` access can view the data associated with the list items but not edit it. |\n| `subsets` | If the value of the subset column is true, the associated item belongs to that subset.  You can view subsets under **Subsets** tab in Anaplan. |\n| `properties` | The value of each custom list property. |\n\n**HIGH VOLUME** \u2014 a list's items can be very large (observed ~9.7 MB for a single list). With no `limit`, a client that auto-follows `meta.paging` merges every page into one response and overflows its token budget. Always pass an explicit `limit` and page with `offset`, and scope to a single list rather than enumerating everything.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "listId",
          "in": "path",
          "description": "List identifier (12-digit numeric string, prefix 101). From `GET /models/{modelId}/lists`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "includeAll",
          "in": "query",
          "description": "When `true`, optional fields are included in the response (for line items, the full metadata field set). When `false` or omitted, only the default fields are returned.",
          "required": false,
          "schema": {
            "type": "boolean"
          },
          "example": true
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "listItems": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string"
                  },
                  "name": {
                    "type": "string"
                  },
                  "code": {
                    "type": "string"
                  },
                  "parent": {
                    "type": "string"
                  },
                  "parentId": {
                    "type": "string"
                  },
                  "listId": {
                    "type": "string",
                    "description": "Anaplan ID of the parent hierarchy list this item belongs to. Only present when the list has a Parent Hierarchy configured."
                  },
                  "listName": {
                    "type": "string",
                    "description": "Name of the parent hierarchy list this item belongs to. Only present when the list has a Parent Hierarchy configured."
                  },
                  "subsets": {
                    "type": "object",
                    "description": "Subset membership flags keyed by subset name. true means the item belongs to that subset. Only present when includeAll=true.",
                    "additionalProperties": {
                      "type": "boolean"
                    }
                  },
                  "properties": {
                    "type": "object",
                    "description": "Custom list property values keyed by property name. Values are string-encoded regardless of the underlying property type. Only present when includeAll=true.",
                    "additionalProperties": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "postWorkspacesByWorkspaceidModelsByModelidListsByListidItems": {
      "description": "Minimum role: Standard User.\n\nAdds items to a list. You must provide the list ID and data for each item. You can add one or more items in each request.\n\n**Notes:**\n\n*   A maximum of 100,000 list items can be added with a single call.\n\n### Request body\n\n| **List Data** | **Description** |\n| --- | --- |\n| `{name}` | The name value for the item in the list. |\n| `{code}` | The code value for the item in the list. |\n| `{parent}` | The name of the parent item in the list |\n| `{properties}` | The value of each custom list property. |\n| `{subsets}` | The value of each custom list subset. |\n\n### Logic for add items\n\nThe add list items behavior uses specific logic with names and codes. This logic varies between [general lists](https://help.anaplan.com/f7f01453-71bc-4046-935a-b9d7cb47d15a-General-Lists) and [numbered lists](https://help.anaplan.com/54b75174-848f-482b-a051-a9a063ece50c-Numbered-Lists) in Anaplan.\n\n### Add items to a general list\n\n| **Scenario** | **Existing** | **Input in API** | **Behavior** | **Result** |\n| --- | --- | --- | --- | --- |\n| Add a new list Item | There are no existing items | name=n1  <br>code=c1 | Success.  <br>A new list item is created. | name=n1  <br>code=c1 |\n| Add list item with name conflicts | name=n1  <br>code=c1 | name=n1  <br>code=c2 | There is a duplicate name. A new list item is not added. | An error message returns. |\n| Add list item with code conflicts | name=n1  <br>code=c1 | name=n2  <br>code=c1 | There is a duplicate name. A new list item is not added. | An error message returns. |\n| Add list item without name | There are no existing items | code=c1 | There is a missing name. A new list item is not added. | An error message returns. |\n\n### Add items to a numbered list\n\nFor a numbered list, the name column is used for the index. The name is automatically generated. When you add an item, the API does not allow a name as an input.\n\n| **Scenario** | **Existing** | **Input in API** | **Behavior** | **Result** |\n| --- | --- | --- | --- | --- |\n| Add a new list Item | There are no existing items | name=n1  <br>code=c1 | A name is not a valid input. A new list item is not added. | An error message returns. |\n| Add a new list item | There are no existing items | code=c1 | Success.  <br>A new item is added. | success code=c1 |\n| Add a list item with a conflicting code | code=c1 | code=c1 | There is a duplicate code. A new list item is not added. | An error message returns. |",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "listId",
          "in": "path",
          "description": "List identifier (12-digit numeric string, prefix 101). From `GET /models/{modelId}/lists`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "action",
          "in": "query",
          "description": "Operation to perform: `add` appends new items, `delete` removes items identified by name, code, or id.",
          "required": true,
          "schema": {
            "type": "string",
            "enum": [
              "add",
              "delete"
            ]
          },
          "example": "add"
        }
      ],
      "request_body": {
        "media_type": "application/json",
        "required": true,
        "schema": {
          "type": "object",
          "required": [
            "items"
          ],
          "properties": {
            "items": {
              "type": "array",
              "description": "Array of items to add or delete.",
              "items": {
                "type": "object",
                "description": "A list item. For add: provide name/code/parent/properties/subsets. For delete: identify by name, code, or id.",
                "properties": {
                  "name": {
                    "type": "string",
                    "description": "Item display name. Required for named lists when adding. Used as the identifier when deleting."
                  },
                  "code": {
                    "type": "string",
                    "description": "Item code. Used as primary identifier for numbered lists."
                  },
                  "id": {
                    "type": "string",
                    "description": "Item ID. Returned by GET list items. Can be used as the identifier when deleting."
                  },
                  "parent": {
                    "type": "string",
                    "description": "Parent item name for hierarchical lists."
                  },
                  "properties": {
                    "type": "object",
                    "description": "Custom list property values keyed by property name.",
                    "additionalProperties": {
                      "type": "string"
                    }
                  },
                  "subsets": {
                    "type": "object",
                    "description": "Subset membership flags keyed by subset name.",
                    "additionalProperties": {
                      "type": "boolean"
                    }
                  }
                }
              }
            }
          }
        }
      },
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "added": {
              "type": "integer",
              "description": "Number of items successfully added. Present when action=add."
            },
            "ignored": {
              "type": "integer",
              "description": "Number of items not processed due to validation failures."
            },
            "total": {
              "type": "integer",
              "description": "Total number of items in the request."
            },
            "failures": {
              "type": "array",
              "description": "Per-item failure details. Omitted from response when all items were added successfully.",
              "items": {
                "type": "object",
                "properties": {
                  "requestIndex": {
                    "type": "integer",
                    "description": "Zero-based index of the failed item in the request array."
                  },
                  "failureType": {
                    "type": "string",
                    "description": "Failure category (e.g. INCORRECT_FORMAT, INCOMPLETE_DATA)."
                  },
                  "failureMessageDetails": {
                    "type": "string",
                    "description": "Human-readable details about the failure."
                  }
                }
              }
            },
            "deleted": {
              "description": "Number of items successfully deleted. Present when action=delete.",
              "type": "integer"
            }
          }
        }
      }
    },
    "putWorkspacesByWorkspaceidModelsByModelidListsByListidItems": {
      "description": "Minimum role: Standard User.\n\nUpdates items in a list. You must provide the model ID and list ID on the request URL. There are two kinds of lists in Anaplan: standard list and numbered list. For a standard list, the API identifies the item using the id, code, or name. For numbered list, the API uses the id or code. If property or subset is not specified in the input, the existing value is not updated. You can update one or more items in each request.\n\nWhen you specify the list item identifier, select one of the appropriate identifiers (id, code, or name). If the request contains multiple identifiers, the API returns an error.\n\n**Notes:**\n\n*   A maximum of 100,000 list items can be updated with a single call.\n    \n\nNote: When an update to a list item completes successfully, all downstream calculations are also complete. This includes cells linked by formulas to the cells being updated.\n\n### Request body\n\n| **List Data** | **Description** |\n| --- | --- |\n| `{name}` | The name value for the item in the list. |\n| `{code}` | The code value for the item in the list. |\n| `{id}` | The Anaplan ID of the item in the list. |\n| `{parent}` | The name of the parent item in the list |\n| `{properties}` | The value of each custom list property. |\n| `{subsets}` | The value of each custom list subset. |\n\n### Update logic\n\nThe update list item endpoint logic varies by the update scenario.\n\n| **Scenario** | **Existing** | **Input in API** | **Behavior** | **Result** |\n| --- | --- | --- | --- | --- |\n| Update list item | name=n1  <br>code=c1 | name=n1  <br>code=c1 | Success | name=n1  <br>code=c1 |\n| Update name | name=n1  <br>code=c1 | name=n2  <br>code=c1 | Success | name=n2  <br>code=c1 |\n| Update code | id=id1  <br>name=n1  <br>code=c1 | id=id1  <br>name=n1  <br>code=c2 | Success | id=id1  <br>name=n1  <br>code=c2 |\n| Update code without existing code | id=id1  <br>name=n1 | id=id1  <br>name=n1  <br>code=c2 | Success | id=id1  <br>name=n1  <br>code=c2 |\n| Update code  <br>(removing existing code) | id=id1  <br>name=n1  <br>code=c1 | id=id1  <br>name=n1  <br>code=\"\" | This scenario does not provide a new code to replace the existing value. An error returns. | error: `INCOMPLETE_DATA` |\n| Update code without an existing code  <br>(assign a code without the id) | name=n1 | name=n1  <br>code=c1 | Success | name=n1  <br>code=c1 |\n| Update code with same name | name=n1  <br>code=c1 | name=n1  <br>code=c2 | The name is already used. An error returns. | error: `INCOMPLETE_DATA` |\n| Update value with code only | name=n1  <br>code=c1  <br>properties{p1=1234} | code=c1  <br>properties{p1=1234} | Success  <br>It's valid to use the code to identify the record. | code=c1  <br>properties {p1=4321} |\n| Update value with name only.  <br>Do not specify the code | name=n1  <br>code=c1  <br>properties{p1=1234} | name=n1  <br>properties{p1=1234} | The code is required to identify the record. An error returns. | error: `INCOMPLETE_DATA` |\n| Update value with name only | name=n1  <br>properties{p1=1234} | name=n1  <br>properties{p1=4321} | Success  <br>When the code is not defined, the API can still update with the name. | name=n1  <br>properties{p1=4321} |\n\n### Response 200 body\n\nThe response body contains a JSON object that contains `total` (total number of items in request), `updated` (number of items updated), `ignored` (number of items ignored), and `failures` fields. On `failures`, each field contains information about any errors encountered while performing the update action. `requestIndex` is the index in the input items array.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "listId",
          "in": "path",
          "description": "List identifier (12-digit numeric string, prefix 101). From `GET /models/{modelId}/lists`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "updated": {
              "type": "integer",
              "description": "Number of items successfully updated."
            },
            "ignored": {
              "type": "integer",
              "description": "Number of items not updated due to validation failures."
            },
            "total": {
              "type": "integer",
              "description": "Total number of items in the request."
            },
            "failures": {
              "type": "array",
              "description": "Per-item failure details. Omitted from response when all items were updated successfully.",
              "items": {
                "type": "object",
                "properties": {
                  "requestIndex": {
                    "type": "integer",
                    "description": "Zero-based index of the failed item in the request array."
                  },
                  "failureType": {
                    "type": "string",
                    "description": "Failure category (e.g. INCORRECT_FORMAT, INCOMPLETE_DATA)."
                  },
                  "failureMessageDetails": {
                    "type": "string",
                    "description": "Human-readable details about the failure."
                  }
                }
              }
            }
          }
        }
      }
    },
    "postWorkspacesByWorkspaceidModelsByModelidListsByListidReadrequests": {
      "description": "Minimum role: Standard User.\n\nInitiates an asynchronous read of all items in the specified list. Poll the returned `url` until `requestState` is COMPLETE, then retrieve pages via the pages sub-resource.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "listId",
          "in": "path",
          "description": "List identifier (12-digit numeric string, prefix 101). From `GET /models/{modelId}/lists`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "listReadRequest": {
              "description": "Async read-request token returned by POST .../lists/{listId}/readRequests. Poll the returned url to retrieve results when requestState is COMPLETE.",
              "type": "object",
              "properties": {
                "requestId": {
                  "type": "string"
                },
                "requestState": {
                  "type": "string",
                  "description": "Current state of the async read operation.",
                  "example": "IN_PROGRESS"
                },
                "url": {
                  "type": "string",
                  "description": "URL to poll for the read-request result or download pages."
                },
                "availablePages": {
                  "type": "integer"
                },
                "listId": {
                  "type": "string"
                },
                "successful": {
                  "type": "boolean"
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidProcessesByProcessidTasks": {
      "description": "Minimum role: Standard User.\n\nThis lists the tasks in the process for the given processID. This returns a JSON array containing the list of tasks in the process definition.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#\n\n**Note:** Returns `200` with an empty task list rather than `404` when the parent object does not exist, or when the caller holds a NO ACCESS role on the model.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "processId",
          "in": "path",
          "description": "Process identifier (12-digit numeric, prefix 118). From `GET /models/{modelId}/processes`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "sort",
          "in": "query",
          "description": "Sort order for the task list. Format: [prefix]field where prefix is - (descending), + (ascending), or omitted (ascending). Documented sortable fields: creationDate, taskId, taskState, progress. Example: -creationDate (newest first), +taskId, taskState. The API does not validate the field name \u2014 unknown fields are silently accepted and appear to fall back to default ordering.",
          "required": false,
          "schema": {
            "type": "string"
          },
          "example": "-creationDate"
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "tasks": {
              "type": "array",
              "items": {
                "description": "A task tracking the execution of a process/import/export/action",
                "properties": {
                  "creationTime": {
                    "type": "integer",
                    "description": "Task creation time in milliseconds since epoch"
                  },
                  "currentStep": {
                    "type": "string",
                    "description": "Human-readable description of the current execution step"
                  },
                  "progress": {
                    "type": "number",
                    "description": "Execution progress as a fraction (0.0\u20131.0). The value is typically 1.0 on completion."
                  },
                  "result": {
                    "description": "The result of a completed task",
                    "properties": {
                      "details": {
                        "items": {
                          "properties": {
                            "localMessageText": {
                              "type": "string"
                            },
                            "occurrences": {
                              "type": "integer"
                            },
                            "type": {
                              "type": "string"
                            },
                            "values": {
                              "items": {
                                "type": "string"
                              },
                              "type": "array"
                            }
                          },
                          "type": "object"
                        },
                        "type": "array"
                      },
                      "duration": {
                        "type": "string"
                      },
                      "failureDumpAvailable": {
                        "type": "boolean",
                        "description": "Whether a failure dump file was generated for this task. When true, the dump can be downloaded via the failure dump endpoint for this object type."
                      },
                      "nestedResults": {
                        "items": {
                          "x-ptc-truncated": "recursive schema, truncated at #/components/schemas/TaskResult"
                        },
                        "type": "array"
                      },
                      "objectId": {
                        "type": "string",
                        "description": "Numeric ID of the Anaplan object (import, export, process, or action) that this task executed against."
                      },
                      "objectName": {
                        "type": "string",
                        "description": "Display name of the Anaplan object that this task executed against."
                      },
                      "startTime": {
                        "type": "string"
                      },
                      "successful": {
                        "type": "boolean"
                      }
                    }
                  },
                  "taskId": {
                    "type": "string"
                  },
                  "taskState": {
                    "description": "Lifecycle state of the task",
                    "enum": [
                      "NOT_STARTED",
                      "IN_PROGRESS",
                      "COMPLETE",
                      "CANCELLING",
                      "CANCELLED"
                    ]
                  }
                }
              }
            }
          }
        }
      },
      "response_truncated": true
    },
    "postWorkspacesByWorkspaceidModelsByModelidProcessesByProcessidTasks": {
      "description": "Minimum role: Standard User.\n\nYou can POST a process task to run a process. This returns a new `taskID`. Once you have started the process, you can use GET to receive a list of tasks as a JSON array. You can use this to monitor the status of each task.\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "processId",
          "in": "path",
          "description": "Process identifier (12-digit numeric, prefix 118). From `GET /models/{modelId}/processes`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "description": "Initial task reference returned by POST .../tasks. Contains the taskId and a URL for polling status.",
          "properties": {
            "creationTime": {
              "type": "integer",
              "description": "Task creation time in milliseconds since epoch."
            },
            "taskId": {
              "type": "string",
              "description": "Unique identifier for the task. Use this to poll GET .../tasks/{taskId}."
            },
            "taskState": {
              "enum": [
                "NOT_STARTED",
                "IN_PROGRESS",
                "COMPLETE",
                "CANCELLING",
                "CANCELLED"
              ],
              "description": "Lifecycle state of the task."
            },
            "taskUrl": {
              "type": "string",
              "description": "URL for polling task status via GET."
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidProcessesByProcessidTasksByTaskid": {
      "description": "Minimum role: Standard User.\n\nIn the response:\n\n*   `currentStep` is the task associated with the specified `taskID`.\n*   `taskState` is the status of the process task.\n*   `progress` is a double that is typically 1.0.\n*   `result` is a JSON object that indicates whether a failure dump file was created, the `objectID`, and whether the task succeeded.\n*   `failureDumpAvailable` indicates whether this process container or an action within it has a failure dump file. **Note:** For a deletion action, `failureDumpAvailable` is always false.\n*   `nestedResults` indicates whether the actions carried out in the process were successful. This contains details in the form of a JSON array, which also contains some debug information when failures occur. Get the failure dump file to view the complete debug information.\n*   `objectID` is the identifier of the process task or a task that it includes. For instance, if `nestedResults` has `failureDumpAvailable` true, you need the `objectID` within that section to obtain the related 'dump' file.\n*   `taskID` is the ID that you specified in the request.\n    \n\nHere is a list of each possible `taskState`:\n\n| **Status** | **Description** |\n| --- | --- |\n| NOT_STARTED | The task is scheduled to run, but has not started yet. |\n| IN_PROGRESS | The task is currently running. |\n| COMPLETE | The task has finished running whether successful or not. |\n| CANCELLING | The task is currently being canceled. Cancellation is not yet complete. |\n| CANCELLED | The task has been canceled and the changes have been rolled back. |\n\n### Headers\n\n*   `Authorization:AnaplanAuthToken {anaplan_auth_token}`\n*   `Content-Type:application/json`\n\n### Parameters\n\n#",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "processId",
          "in": "path",
          "description": "Process identifier (12-digit numeric, prefix 118). From `GET /models/{modelId}/processes`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "taskId",
          "in": "path",
          "description": "Task identifier, scoped to the parent action/import/export/process that launched it: a taskId is valid only under the same parent, so an action's taskId will not resolve under a process, import, or export. Returned when a run is launched by `POST /workspaces/{workspaceId}/models/{modelId}/imports/{importId}/tasks` (or the equivalent actions, exports, or processes tasks endpoint), and listed by the matching `GET .../tasks` \u2014 `tasks[].taskId`, except the import tasks response, which nests it under `task.taskId`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "task": {
              "description": "A task tracking the execution of a process/import/export/action",
              "properties": {
                "creationTime": {
                  "type": "integer",
                  "description": "Task creation time in milliseconds since epoch"
                },
                "currentStep": {
                  "type": "string",
                  "description": "Human-readable description of the current execution step"
                },
                "progress": {
                  "type": "number",
                  "description": "Execution progress as a fraction (0.0\u20131.0). The value is typically 1.0 on completion."
                },
                "result": {
                  "description": "The result of a completed task",
                  "properties": {
                    "details": {
                      "items": {
                        "properties": {
                          "localMessageText": {
                            "type": "string"
                          },
                          "occurrences": {
                            "type": "integer"
                          },
                          "type": {
                            "type": "string"
                          },
                          "values": {
                            "items": {
                              "type": "string"
                            },
                            "type": "array"
                          }
                        },
                        "type": "object"
                      },
                      "type": "array"
                    },
                    "duration": {
                      "type": "string"
                    },
                    "failureDumpAvailable": {
                      "type": "boolean",
                      "description": "Whether a failure dump file was generated for this task. When true, the dump can be downloaded via the failure dump endpoint for this object type."
                    },
                    "nestedResults": {
                      "items": {
                        "x-ptc-truncated": "recursive schema, truncated at #/components/schemas/TaskResult"
                      },
                      "type": "array"
                    },
                    "objectId": {
                      "type": "string",
                      "description": "Numeric ID of the Anaplan object (import, export, process, or action) that this task executed against."
                    },
                    "objectName": {
                      "type": "string",
                      "description": "Display name of the Anaplan object that this task executed against."
                    },
                    "startTime": {
                      "type": "string"
                    },
                    "successful": {
                      "type": "boolean"
                    }
                  }
                },
                "taskId": {
                  "type": "string"
                },
                "taskState": {
                  "description": "Lifecycle state of the task",
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ]
                }
              }
            }
          }
        }
      },
      "response_truncated": true
    },
    "postWorkspacesByWorkspaceidModelsByModelidViewsByViewidReadrequests": {
      "description": "Minimum role: Standard User.\n\nStarts a large-volume read request of view data. This enables you to read data from views that are larger than a million cell count.\n\n**Note:**\n\n*   As a best practice, use the Delete read requests call to clear all pages from completed exports as soon as you download all pages. Doing so will make space available for future exports.\n\n### Request Body\n\nThis API supports exporting data in the following formats:\n\n| **Type** | **Description** | **More information** |\n| --- | --- | --- |\n| `GRID_ALL_PAGES` | This layout respects the existing sorting and filtering you have imposed on a data grid before you launch the export. | [Export grid](https://help.anaplan.com/Import_and_Export/Export_Layouts/Export_Data_in_Grid_Layout.html) |\n| `TABULAR_SINGLE_ITEMS` | This layout takes all dimensions and puts them in columns, with the final column reserved for data. | [Export tabular single column](https://help.anaplan.com/Import_and_Export/Export_Layouts/Export_Data_in_Tabular_Single_Column_Layout.html) |\n| `TABULAR_MULTI_COLUMN` | This layout enables you to export multiple data columns. If you set up a Boolean line item, you can use it as a filter to control which data is exported. This is especially useful when exporting from large modules. It saves the manual task of marking data cells for export. | [Export Tabular Multiple Column](https://help.anaplan.com/Import_and_Export/Export_Layouts/Export_Data_in_Tabular_Multiple_Column_Layout.html) |",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "viewId",
          "in": "path",
          "description": "View identifier. From `GET /models/{modelId}/views` or `GET /models/{modelId}/modules/{moduleId}/views`. A line item ID from `GET /models/{modelId}/modules/{moduleId}/lineItems` also works when that line item has a subsidiary view, returning that view's data.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "viewReadRequest": {
              "description": "Result record for a large-view read operation. Tracks the asynchronous read state and provides a URL for downloading the data once it is ready.",
              "properties": {
                "availablePages": {
                  "type": "integer"
                },
                "requestId": {
                  "type": "string"
                },
                "requestState": {
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ]
                },
                "successful": {
                  "type": "boolean"
                },
                "url": {
                  "type": "string"
                },
                "viewId": {
                  "type": "integer"
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidViewsByViewidReadrequestsByRequestid": {
      "description": "Minimum role: Standard User.\n\nReturns the status of an export action after a bulk data long request starts. You can use it to check how many pages are available to download.\n\n### Response scenarios\n\n*   When `requestState` is `COMPLETE` and `successful` is `true` , the export has completed successfully and the user can download all the pages.\n*   When `requestState` is `IN_PROGRESS` and `successful` is `false`, the export has not yet completed. You can download the available pages indicated by the `availablePages` field.\n*   When `requestState` is `COMPLETE` and `successful` is `false`, this means the export failed. Retry and initiate a new export.\n*   When 'requestState' is 'NOT_STARTED' then simply check the status again.\n*   When requestState' is 'CANCELLED' then this request is completed by a 'DELETE' call.\n    \n\nWe recommend you delete the last failed export before initiating a new export, using the delete export endpoint.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "viewId",
          "in": "path",
          "description": "View identifier. From `GET /models/{modelId}/views` or `GET /models/{modelId}/modules/{moduleId}/views`. A line item ID from `GET /models/{modelId}/modules/{moduleId}/lineItems` also works when that line item has a subsidiary view, returning that view's data.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "requestId",
          "in": "path",
          "description": "Read request identifier returned by `POST /workspaces/{workspaceId}/models/{modelId}/views/{viewId}/readRequests`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "viewReadRequest": {
              "description": "Result record for a large-view read operation. Tracks the asynchronous read state and provides a URL for downloading the data once it is ready.",
              "properties": {
                "availablePages": {
                  "type": "integer"
                },
                "requestId": {
                  "type": "string"
                },
                "requestState": {
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ]
                },
                "successful": {
                  "type": "boolean"
                },
                "url": {
                  "type": "string"
                },
                "viewId": {
                  "type": "integer"
                }
              }
            }
          }
        }
      }
    },
    "deleteWorkspacesByWorkspaceidModelsByModelidViewsByViewidReadrequestsByRequestid": {
      "description": "Minimum role: Standard User.\n\nDeletes or cancels a read request that started. This removes all the pages from the file store and stops the ongoing read request.\n\n**Notes**:\n\n*   As a best practice, use this call to clear all pages from completed exports as soon as you download all pages. Doing so will make space available for future exports.\n*   The system will automatically reclaim space for read requests not accessed for over 30 minutes. The same applies to failed read requests.\n\n### Response 200 body",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "viewId",
          "in": "path",
          "description": "View identifier. From `GET /models/{modelId}/views` or `GET /models/{modelId}/modules/{moduleId}/views`. A line item ID from `GET /models/{modelId}/modules/{moduleId}/lineItems` also works when that line item has a subsidiary view, returning that view's data.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "requestId",
          "in": "path",
          "description": "Read request identifier returned by `POST /workspaces/{workspaceId}/models/{modelId}/views/{viewId}/readRequests`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "viewReadRequest": {
              "description": "Result record for a large-view read operation. Tracks the asynchronous read state and provides a URL for downloading the data once it is ready.",
              "properties": {
                "availablePages": {
                  "type": "integer"
                },
                "requestId": {
                  "type": "string"
                },
                "requestState": {
                  "enum": [
                    "NOT_STARTED",
                    "IN_PROGRESS",
                    "COMPLETE",
                    "CANCELLING",
                    "CANCELLED"
                  ]
                },
                "successful": {
                  "type": "boolean"
                },
                "url": {
                  "type": "string"
                },
                "viewId": {
                  "type": "integer"
                }
              }
            }
          }
        }
      }
    },
    "getWorkspacesByWorkspaceidModelsByModelidViewsByViewidReadrequestsByRequestidPagesByPageno": {
      "description": "Minimum role: Standard User.\n\nDownloads the available pages, either when the read request is in progress, or when the request is complete. This request returns a CSV format response of the export list items.\n\nYou can download pages while the export is in progress. You can download up to `availablePages` number of pages returned by the export status API.\n\nNote that page numbers start with zero. That means if 10 pages are available, you can download from page=0 through page=9\n\n**Note:**\n\n*   As a best practice, use the **Delete read requests** call to clear all pages from completed exports as soon as you download all pages. Doing so will make space available for future exports.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "modelId",
          "in": "path",
          "description": "Model identifier (32-character uppercase hex string). From `GET /models` or `GET /workspaces/{workspaceId}/models`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "viewId",
          "in": "path",
          "description": "View identifier. From `GET /models/{modelId}/views` or `GET /models/{modelId}/modules/{moduleId}/views`. A line item ID from `GET /models/{modelId}/modules/{moduleId}/lineItems` also works when that line item has a subsidiary view, returning that view's data.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "requestId",
          "in": "path",
          "description": "Read request identifier returned by `POST /workspaces/{workspaceId}/models/{modelId}/views/{viewId}/readRequests`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "pageNo",
          "in": "path",
          "description": "Zero-based page index of a read request result set.",
          "required": true,
          "schema": {
            "type": "string"
          },
          "example": "0"
        },
        {
          "name": "Accept-Encoding",
          "in": "header",
          "schema": {
            "type": "string"
          },
          "example": "csv"
        }
      ],
      "request_body": null,
      "response": null,
      "response_note": "no documented 2xx JSON response schema \u2014 probe a read operation to learn it"
    },
    "getWorkspacesByWorkspaceidVisitors": {
      "description": "Minimum role: User Administrator.\n\nReturns all visitors for a workspace. Requires tenant admin privilege. Workspace admin access alone is insufficient.\n\nDeactivated visitors (`active` is `false`) are typically not relevant to callers listing people. Filter to `active=true` unless you specifically need inactive visitors.",
      "parameters": [
        {
          "name": "workspaceId",
          "in": "path",
          "description": "Workspace identifier (32-char lowercase hex). When the path also has a `modelId`, use that model's `currentWorkspaceId` (`GET /models/{modelId}`, or `GET /models` matched by `name`). For workspace-only endpoints, scan `GET /workspaces` by `name`.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "offset",
          "in": "query",
          "description": "Zero-based index of the first result to return. Use with `limit` to page through results. The response echoes the applied offset in `meta.paging.offset`, and when more results remain the server supplies a ready-to-use next-page URL in the `next` field of `meta.paging`.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 0
          },
          "example": 0
        },
        {
          "name": "limit",
          "in": "query",
          "description": "Maximum number of results to return per page. Use with `offset` to page through results. When omitted, the server returns up to a default page size of 5000. Compare `meta.paging.currentPageSize` against `meta.paging.totalSize` to detect whether further pages exist.",
          "required": false,
          "schema": {
            "type": "integer",
            "minimum": 1
          },
          "example": 100
        }
      ],
      "request_body": null,
      "response": {
        "status": "200",
        "media_type": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "meta": {
              "description": "Response metadata envelope present in paginated Anaplan API responses. `schema` is a URL to the JSON schema definition for the response body. `paging` holds cursor-based navigation state.",
              "type": "object",
              "required": [
                "schema"
              ],
              "properties": {
                "schema": {
                  "type": "string",
                  "description": "URL to the JSON schema definition for this response body. A URL string, not an inline JSON Schema object."
                },
                "paging": {
                  "type": "object",
                  "required": [
                    "currentPageSize",
                    "totalSize",
                    "offset"
                  ],
                  "properties": {
                    "currentPageSize": {
                      "type": "integer",
                      "description": "Number of results in this page. When `limit` is omitted the server returns up to a default page size of 5000."
                    },
                    "totalSize": {
                      "type": "integer",
                      "description": "Total number of results matching the query, across all pages."
                    },
                    "offset": {
                      "type": "integer",
                      "description": "Zero-based index of the first result in this page."
                    },
                    "next": {
                      "type": "string",
                      "description": "Ready-to-use URL for the next page of results, echoing the applied `limit` and `offset` query parameters. Present only when further results remain.",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=20"
                    },
                    "previous": {
                      "type": "string",
                      "description": "Ready-to-use URL for the previous page of results, echoing the applied `limit` and `offset`. Present only when paging past the first page (offset greater than 0).",
                      "example": "https://api.anaplan.com/2/0/models?limit=20&offset=0"
                    }
                  }
                }
              }
            },
            "status": {
              "description": "Response status object present in every Anaplan API response envelope. `code` is the Anaplan-specific status code (not the HTTP status code). `message` is the human-readable outcome.",
              "type": "object",
              "properties": {
                "code": {
                  "type": "integer",
                  "description": "Anaplan API status code for this response. Distinct from the HTTP status code.",
                  "example": 200
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable description of the status outcome.",
                  "example": "Success"
                }
              }
            },
            "visitors": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "firstName": {
                    "type": "string"
                  },
                  "lastName": {
                    "type": "string"
                  },
                  "email": {
                    "type": "string",
                    "pattern": "^[_A-Za-z0-9-\\\\+]+(\\\\.[_A-Za-z0-9-]+)*@[A-Za-z0-9-]+(\\\\.[A-Za-z0-9]+)*(\\\\.[A-Za-z]{2,})$"
                  },
                  "active": {
                    "type": "boolean"
                  },
                  "otherWorkspaces": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
